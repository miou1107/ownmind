Personalized persistent memory for AI

[English](../README.md) | [繁體中文](README.zh-TW.md) | [日本語](README.ja.md)

**現在のバージョン：v1.30.8** · 詳細は [CHANGELOG](../CHANGELOG.md) を参照

# OwnMind — クロスプラットフォーム AI メモリ＆鉄則執行システム

AI に好みを忘れさせない、同じ低レベルなミスを繰り返させない。Claude Code（Claude のコマンドライン版）、Codex（OpenAI のコマンドラインツール）、Cursor（AI エディタ）、Gemini CLI（Google のコマンドラインツール）、その他のオンライン AI を使っていても、OwnMind はあなたの開発の好み、プロジェクトの文脈、絶対に違反できない「鉄則」（剛性規則：違反不可の硬性規定）をすべてのツール・マシン・AI モデル間でロックします。

---

## OwnMind は次の 4 タイプに最適

### 👤 個人開発者 — 好み設定は一度きり、どこでも適用

会社の PC で設定した「commit message（コミットメッセージ：各 git 履歴に書く説明）は繁体字中国語で統一」というルール。家のノート PC で新しい会話を開いても、出張先で借りた PC でも、AI が自動的に守ります。**あなたの開発人格は環境を変えてもリセットされません。**

### 🔁 マルチツール派 — Claude / Cursor / Codex / Copilot を同じルールで

朝は Claude Code でバックエンド、午後は Cursor でフロントエンド、夜は Codex CLI でスクリプト。各ツールが同じ個人の好みと鉄則を自動ロードし、**ツールごとに教え直す必要はありません**。来月新しいエディタ（OpenCode、Windsurf、何でも）を追加しても、OwnMind に接続すれば全て継承します。

### 👥 チームリード — ルール一括配信＋ダッシュボードで実コンプライアンス可視化

管理コンソールでチームルールを公開（例：「PR にはテスト必須」）すれば、全メンバーの AI が自動ロード。ローカルの pre-commit hook（pre-commit フック：git commit を実行する前に差分を検査する小プログラム）が非コンプライアンスコードを commit させません。管理ダッシュボード（データダッシュボード：データを図表化した Web ページ）はメンバー毎・ルール毎の実コンプライアンス率、チームで最も頻発する落とし穴、`OWNMIND_BYPASS`（ルールバイパス環境変数）の使用頻度トップを表示 — **勘ではなくデータでチーム管理できます。**

### 🛡️ セキュリティ意識者 — AI から機密データが漏れない

- パスワード / API キー（オンラインサービス呼び出し用の認証情報）を git commit しようとする → pre-commit hook がパターンマッチでブロック（v1.19.7 新規）
- AI がパスワードをメモリに書こうとする → メモリ API が HTTP 400 で拒否し暗号化金庫へ誘導
- AI 返答に user の身分証 / メール / 携帯番号が紛れる → reply-lint（返答品質リンター：AI 返答のルール違反を自動スキャン）が `privacy_check` イベントを発火、ブロックするかは各 user 自身の鉄則が決める（v1.19.7 で検出器追加、v1.19.10 で中立化）

金融、医療、法務などセンシティブな業界の開発者に特に有用。

---

## OwnMind 導入前後の開発体験比較

AI は強力ですが **本質的に長期記憶を持たず**、異なる AI モデル（例：Anthropic の Claude と Google の Gemini）はルール解釈の仕方が大きく異なります。複数マシンを跨ぐと摩擦はさらに増します。導入前後の典型的な体験比較：

### ペイン 1：新ウィンドウ開く度に「好みを再説明」

- **新しい一行が一つの言語で固定されていた** — v1.26.151 は五つの種別名を中国語の文字列リテラルとして書いた。その一行を求めた issue は、同じ文の中で逆のことを求めていた。原稿は英語で書き、配信時にモデルが訳す — 起動時のヒント（`hooks/lib/render-session-context.js`）ですでに使われている経路である。その経路を示す括弧書きは読み飛ばされ、隣の相互参照は別件の閉じられた issue を指しており、「たどるべき経路がない」が保留の理由になった。経路はその文の中にあったのに。いまラベルは英語の原稿であり、描画された行には利用者自身の言語で届けるための指示が付く。指示は翻訳を越えて保たれるべきものを名指しする。件数とバージョン表記を固定しなければ、モデルは数字を言い換えて消してしまう。そして数字こそがすべての要点であり、「見たが該当なし」と「そもそも尋ねていない」を分けるものである。さらにテストがすべてのラベルを走査し、CJK 文字があれば失敗する。次に種別を追加する人が、静かに一つの言語を原稿へ固定してしまうことはできない。退避経路にもとからある中国語は意図的に触れていない。更新前のサーバーを使う利用者がずっと読んできたものであり、それまで訳せば、劣化した経路を誰も求めていない第三の姿に変えることになる。`v1.26.152`
- **リマインダーは五つの記憶種別のうち一つしか見ていなかった** — 危険な操作の前にフックが一行を出す。その一行には三つの仕事がある。OwnMind が何が起きていると考えているかを言うこと、利用者の記憶を実際に参照したと示すこと、そして中身を読む道を残すこと。どれも果たしていなかった。出力は `鉄則発動（install）` だけ — 内部のトリガー名、件数なし、次の一手なし — しかも二つ目は描画ではなくデータの段階で不可能だった。どちらのフックも `/api/memory/type/iron_rule` しか取得していなかったからである。利用者は「この操作に該当するチーム規約はない」と「チーム規約はそもそも見ていない」を区別できなかった。これは別々の事実であり、一方は不具合であり、表示はどちらが起きたのかを言う手段を持っていなかった。ラベルも同じ失敗の縮図だった。認証ヘッダーを調査していた利用者は、`X-API-KEY` を伴うすべての `curl` の下に `鉄則発動（install）` が現れるのを見て、何をインストールしているのかと尋ねた。何もしていない。`install` はたまたま鍵の作業を含む内部の区分であり、人に見せるべきものではなかった。新しいエンドポイントが五種別すべてをトリガーで絞り込んだうえで一度の要求で返す。これはあらゆる危険なコマンドの前に置かれ、シェルフックの curl は同期で上限 5 秒なので、五回直列にすれば `git commit` の前に最悪 25 秒が挟まる。その遅延こそ仕組み全体が無効化される道筋である。四種別は中身ではなく件数を送る。コマンドの前に数百行の見出しが並ぶのは、リマインダーが読み飛ばされるものへ変わる過程そのものだからだ。タグのない行が新しい四種別を埋め尽くすこともなくなった — ある実アカウントで commit は 63 から 38 へ、install は 38 から 13 へ減り、鉄則は一つも失われなかった。「タグがなければすべてに該当」は鉄則しかない経路のために書かれた規則で、プロジェクトのメモには通用しない。件数 0 の種別も引き続き表示する。その 0 こそ情報であり、古い表示が組み立てられなかった文である。シェルフックの埋め込み描画は独立したファイルへ移り、そこが抱えざるを得なかったトリガー別名表の写しは不要になった。二つのフックは同じ一行を出すようになり、同じ考えの二通りの翻訳ではなくなった。そしてフックとサーバーは別々に配備されるため、200 以外の応答はどのサーバーにもある旧エンドポイントへ退避し、その退避を活動ログに記録する — 永続的に劣化していながら何も言わないリマインダーは、正常なものと見分けがつかない。退避時に問い合わせていない四種別を 0 として表示することはしない。それは修正の側が同じ嘘をつくことになる。`v1.26.151`
- **二重の判断を一つにまとめた** — v1.26.149 は二つのコマンド分類器に同じ答えを出させたが、一つのものにはしなかった。別々に編集できる状態が残っていたからだ。残る問いは一つ、シェル版は今も node なしで動く必要があるのか。答えは否であり、ずっと以前から否だった — `hooks/ownmind-iron-rule-check.sh` は node を十数回呼び、その最初の一回はファイルの冒頭近く、分類の前に置かれ、`package.json` からバージョン文字列を読むためだけに存在する。認証情報の読み取り、鉄則の取得、入力の解析、検証、出力の描画もすべて node を通る。node のない機械が得るのは機能を削られたフックではなく、23 行目で落ちるフックだ — つまり重複を取り除く代償はすでに支払われており、その重複は何も買っていなかった。grep の連鎖は削除した。シェルフックは今、コマンドを四行のラッパーへパイプで渡し、そのラッパーが `.js` フックの import するのと同じ `detectCommandTrigger()` を呼ぶ。argv ではなく stdin を使うのは、`git commit -m` のメッセージが日常的に複数行になり、stdin には長さの上限も引用符の規則もないからである。ラッパーの終了ステータスは検査し、その stderr は意図的にそのまま流す。静かに壊れた分類器は空の答えを返し、それは「このコマンドは何も発動させない」— 最も多い正しい答え — と見分けがつかず、欠陥にとって最良の隠れ場所になるためだ。そしてこの失敗モードが実際に現れた。変更後の最初の実行では**すべての**コマンドが「トリガーなし」に分類された。テストがシェルフックのために用意する使い捨ての `$HOME` に、新しいファイルへのリンクがなかったからである。四つのファイルにまたがる八件のテストが、たった一つの欠けたシンボリックリンクで赤くなった — 同じ準備処理が五つのテストファイルに手で写されており、五か所すべてを覚えていた者がいなかったからだ。これは同じ欠陥が一層下で起きたものなので、準備処理は `tests/helpers/hook-home.js` へ移し、そこが一覧を持つ。トリガーの照合パターンは一つも変えていない。防護テストの 18 コマンドはすべて以前と同じに分類される。`v1.26.150`
- **同じ判断が二重に書かれ、答えが食い違っていた** — 危険なコマンドを実行する前に、フックが該当する鉄則を挙げる。「どの鉄則が該当するか」という判断は二か所に書かれている。一つは `shared/helpers.js`、もう一つはシェル版の中に手書きされた `grep` の連鎖で、mac と Linux に `install.sh` が登録するのは後者だ。どちらの存在にも理由がある —— シェル版は `PATH` に node がなくても動き、node にパスを渡す操作（Windows で二度の沈黙した失敗を生んだ動き）を避けている —— が、両者が同じ答えを返すことを確かめるものは何もなかった。隣にある `KEEP IN SYNC` の注記が覆っていたのはタグ別名表だけで、分類そのものは無防備であり、その無防備な方こそ大半の利用者が実際に動かしている実装だった。17 のコマンドのうち **7 つで答えが違い**、うち三つは見解の相違ではなく実際の不具合である。`git tag` は mac と Linux でどのトリガーにも届かず、バージョン同期の鉄則が最も語るべき瞬間にフックは黙っていた。`docker compose build` と `docker compose push` も同様で、デプロイの照合が `docker.*up` でありどちらも含まないため、あるコマンドのために書かれた鉄則が、そのコマンドを見ている実装からは不可視だった。逆方向では `docker.*up` が `docker logs backup` と `docker ps | grep uptime` に一致した。`backup` と `uptime` の中に `up` があるからで、ログを読むだけでデプロイ用の鉄則一覧が並んだ。防護テストは最初に書き、最初に走らせた —— 7 件が赤、予測どおりの 7 件である。テストは実物のシェルフックを起動し、フック自身が出力する見出しからトリガーを読み取る。照合パターンを書き写さないのは、書き写したテストが著者の読んだ側の写しに味方する第三の複製になるからで、見出しを解釈できなければ「トリガーなし」に落とさず throw する —— 落とせばファイル全体が緑のまま何も測らないことになる。連鎖は現在、順序も含めて参照実装の逐語的な書き写しである。一つのコマンドが二つの系統に属することがあり、先に一致した方が勝つためだ。一つの照合だけは逆向きに移した。`docker stack deploy` はシェル版にしかなく、削るのではなく参照実装へ引き上げた —— Swarm のデプロイもデプロイだからである。重複そのものは残る。解消にはシェル版が node から独立している理由が今も成り立つかという判断が要り、その判断は防護テストが揃ってから下す方がよい。どちらの道もまずそれを満たさねばならないからだ。`v1.26.149`
- **あなたの会社で実際に頼めることを名指しする一行** — 応答の末尾には毎回ひとことの助言が付く。その一つは「チーム規範という仕組みがある」と告げるだけで、構成員が実行できることを何も伝えていなかった。2026-08-11 に`pages.fontrip.com` へ公開するチーム規範が登録され、以後は同僚が「幫我發 pages」と言えば一連の手順が代行される —— 誰かが口で伝えない限り、本人には知りようがない。題名をそのまま読み上げる当初案は、実データに当てて棄却した：32 件のうち 17 件は AI が黙って従う規律、8 件は AI が読むための内容、頼めるものは 6 件。六度に五度は雑音であり、題名は規範を管理する側が見分けるための文字列で、一件は括弧の途中で切れる。そこで規範自身が二つを宣言する：`user_invocable` と、利用者の言葉で書いた一文 `invocation_hint`。一文なしの標識は書き込み時に拒否し、書くべき文例を返す —— 標識だけでは題名の読み上げに退化し、気づくのは標識を立てた人ではなく助言を見た人になるからである。これらの文は静的なチーム規範の助言と**置き換わる**。追加にすれば、六件の会社は 25 行の四分の一が自社の文になり、二十件なら製品自身の助言が埋もれる。推測は一切しない：未標識の規範は決して現れない。見落としの代価は宣伝されない能力一つ、誤推測の代価は全構成員が毎回、実行できない一文を読むことである。標識が一つもない利用者に見えるものは以前と同一である。二度の査読が、この機能が「出荷されたが誰にも届かない」形になる二つの経路を突き止めた：二つの欄の存在を製品のどこも告げておらず、管理者が AI に標識を頼んでも手がかりが無かったこと（現在は両ツールの説明が例文つきで述べる）。そして応答ごとの助言は `ownmind_init` からしか一覧を学ばず、Claude Code は SessionStart フックで記憶を読み込むためそのツールを呼ばない —— 利用者が最も目にする面で一覧は空のままだった。現在はフックがディスクへ書いた内容から補う。さらに複線の一文が画面へ届くことも判明した：書き込み側は改行を拒むが、読み出し側の二つの濾過は空と長さしか見ず、`trim()` は内部の改行に触れない —— しかもその一文は他の構成員の AI が復唱するよう指示される文である。検証：新規 35 件（6 つの変異が赤くなることを確認）、全体 4,712 件が緑。`v1.26.148`
- **チーム規範は、作った本人にしか変えられなかった** — issue #85 は各規範に印を付けることを要するが、最初の一件で 404 になった。本番に対し super_admin で計測：規範 108 の読み取りは 200、書き込みは 404 Memory not found。書き込み経路はいずれも `WHERE id = $1 AND user_id = $2` で始まり、「チーム規範は管理者のみ編集可」という検査はその三十行下にある —— 所有者でない者は既に 404 で去っており、その検査が実際に阻んでいたのは「管理者でない所有者が自分の規範を編集する」場合だけだった。文面が示す権限は一度も存在しなかった。計測した代償：32 件の規範が 2 つのアカウントに分かれ、issue #85 が印を付けたい 6 件も両者に跨がるため、**すべてに印を付けられるアカウントは一つもない**。作成者が退職すれば、その規範は訂正も停止もできないまま、全員の AI へ会社の方針として配られ続ける。さらに停止は管理者を要し、再有効化は要さなかった —— 一方向しかない扉である。今は一つの判定が「この呼び出し元はこの行に書けるか」に答える：所有者、または管理者 —— 対象は元より全員が読める二つの型のみ。管理者でも他人の鉄則には 404 を返し、存在しない id への応答と一字も違わないため、この API で他人の記憶を数え上げることはできない。到達しなかった検査はそのまま動くようになり、再有効化と復元を初めて覆う。書き込みは呼び出し元ではなく行の所有者を束縛する —— 呼び出し元を束ねれば同じ 404 が一文あとに、しかも無言で起きる。管理者による書き込みは「誰が、誰の行を」変えたかを版履歴に残し、所有者自身の書き込みは履歴の形を変えない。issue #85 自体はまだ実装していない。これはその前にあった壁を取り除くものである。二名の査読者はいずれも「触れてはならない行に届く経路」を見つけられなかったが、四件を指摘した：新設の「誰が変えたか」の記録は、記録される当人が偽造できた（メモリの付加欄は元より利用者が書ける値で、履歴へは丸ごと引き継がれていた）／その監査の試験は条件を定数に変えても緑のままだった／停止経路の検査だけ試験が範囲を絞っておらず、反転させても（構成員は通り、管理者は拒まれる）全件緑だった／検査が書き込みより先に走ることを何も固定していなかった —— それこそ本版が直している欠陥である。検証：新規 37 件（6 つの変異が実際に赤くなることを確認）、全体 4,676 件が緑、および本番データベースに対する三項目（新しい検索が他アカウントの行を見つける／所有者束縛の書き込みが当たる／旧述語は 0 件 —— それこそが 404 の理由）。`v1.26.147`
- **一行にしか読めない規範** — チーム規範の保存のされ方は二通りある。本文がその行自身にある場合と、アップロード時に細片へ分割され、その行には「ownmind-upload が自動作成した規範の要約」という一文だけが残る場合である。読み方は id による一つだけで、後者はその一文を返していた。**足りていないという印はどこにもない** —— 短い規範と、短くしか読めなかった規範とが見分けられない。同僚が「幫我 push 一下」と言えば、AI は `git-push` の規範を認め、指示どおりに読み、一文を受け取り、いつもどおり push する。その規範が定める六つの手順は一つも適用されず、誰も気づかない。報告元のアカウントでは七件がこの形である（issue #89）。これは既に二度、より良い一文で答えられてきた。v1.26.141 の書き換えは一方の形を直し、他方を壊した。一つの指示が両方の形について真であることはできず、セッション文脈に入る指示は一つだけだからである。よって読み取りが細片そのものを返す：`GET /api/memory/:id` は細片が存在すれば `fragments` として付す。その行自身の `content` には触れない —— そこへ併合すれば、`ownmind_get` 自身の「更新の前に全文を読め、さもなくば残りを上書きする」という一文が、全細片を親へ潰し込めという指示に変わる。併合するのは一件を読む経路だけである。32 件の一覧は 38,108 文字のままで、約 74,000 文字にはならない。順序は行 id をやめた。見出しの改名は「旧行の無効化と新行の挿入」であり、上位見出しを改名すればその配下すべてが末尾へ移る。同期が各断片の文書内位置を記録し、未同期の規範は従来どおりに退避する。20,000 文字を超えれば打ち切りを明示し、続きへの呼び出しを示す（現在の最大は 14,091 文字）。検証：単体 17 件と、実際に赤くなることを確かめた七つの変異、および実際の Postgres に対する 11 項目（文書順、本文のない節、無効化された細片、非所有者の読み取り、離脱した構成員）。`v1.26.146`
- **戸を守っていたものが、外の者に消されていた** — 三つのプログラムが `~/.ownmind/.update-lock` を共有し、同じディレクトリで `git pull` を走らせるのを一度に一つに限っている。十分より古い錠は死んだ実行のものとみなして回収する。経路の削除と再作成は不可分にできないため、回収は二つ目のファイルの背後に直列化されている。削除が起きる唯一の区間のための相互排他である。CI がその区間に二つのプロセスがいるのを捉えた（同一コミットの再実行では通ったので負荷依存である）。十六の競合者と CPU 飽和で再現した：**500 回中 56 回の二重取得**、そして追跡が示したのは一人用の区間に三つのプロセスがいる姿だった。*放置された*目印を片づける操作自体が削除と再作成であり、生きている目印に当たる。v1.26.111 はその半分を見て、生きた目印を動かしたプロセスを降ろした。それはそのプロセスを締め出すだけで、すでに削除された目印については何もしない。目印は相互排他そのものであるから、その持ち主は守りのない区間に取り残され、次の到着者がそこへ加わる。以後それぞれが、他者の新しい錠が存在する前に読んだ齢を根拠に錠を削除する。共有コメントはこの残余を「自らの検査の数マイクロ秒後」と呼んでいたが、実測では全体の 11% だった。現在、目印は錠と同じく持ち主の合言葉を帯び、その持ち主は錠を削除する直前と目印を外す直前に、目印がなお自分のものであることを確かめる。同じ条件で **500 回中 0 回**。他に二つの案を試し、いずれも不具合そのものより悪いと測れた（生きた目印を戻す：120 回中 45 回、戻す動作自体が相互排他の不在する第二の窓であり `rename` は上書きする。測る前に錠を経路から外す：120 回中 53 回、生きた錠が試行のたび一時的に消える）。どちらも次に思いつく案なのでコードに記録した。両実装を同時に変更し、回帰試験は巡り合わせではなく挿入した停止で窓を開き、それぞれ「何も回収しなければ落ちる」対照を伴う。試験が持ち出すシェル関数の一覧はソースから生成されるようになった。補助関数を一つ加えただけで全競合者が `command not found` で死に、それが「錠は誰も通さなかった」と読めていたためである。`v1.26.145`
- **更新器が自分の出力を「あなたの変更」と呼んでいた** — 更新は最初に「この作業ディレクトリは編集されたか」を問い、はいなら退避し、`upgrade_dirty_tree` を報告し、`git reset --hard origin/main` を実行する。2026-08-11、誰も触っていない導入環境で測ったところ答えは「はい」だった：` M hooks/ownmind-usage-scanner.js`、`?? bin/`、`?? reports/` — 三つとも OwnMind 自身が書いたものである。追跡下の一つは永久に繰り返す：このファイルは `100644` で記録され、`install.sh` と `scripts/update.sh` の両方が `chmod +x` するため、reset が `644` に戻し、同じ更新の最後で同期スクリプトが再び `755` にする。未追跡の二つはそもそも消えない。`git reset --hard` は未追跡ファイルを削除しないからである — ある利用者の機械が毎回 `?? standards/` を報告し続けているのはこれが理由である。代価は上書きそのものではない（先に退避される）。上書きする分岐が既定の分岐になり、その警告が毎回出る雑音になったことであり、そのため本当の編集も同じ文面の中を流れていく。現在は実行ビットを記録し、判定は追跡下の変更のみを見る（未追跡の経路は記録には残すが、それを理由に何も上書きしない）。さらに `chmod +x` の対象一覧を導入スクリプトから生成する試験を追加した — 手書きの `.gitignore` の一覧は、誰かが最後に見てから増えた分だけ、二度足りていない。これを追う途中で見つかった、より重い一件：`hooks/ownmind-session-start.sh` は pull の前に `git stash` を実行し、成功経路を含めどの出口にも `stash pop` がなかった。v1.17.22 が MCP で出したのと同じ不具合であり、そのときは実際に未コミットの作業が消えた。修正は MCP には入り、このフックには届いていなかった。ある機械には 30 件の stash が残っていた（更新一回につき一件、v1.17.x まで遡る）。現在は `--autostash` で引く。加えて、リポジトリ内のどのスクリプトでも stash して戻さなければ落ちる、一覧を自動生成する試験を追加した。 `v1.26.144`

- **v1.26.139 のフレーク修正は原因を誤っていた** — あの版はテストのランダムな赤を競合状態と読み（並列負荷で `address()` が使える値を返さず `undefined` が URL に入る）、待機とアドレス検査を追加し、三回連続で同一の結果を見て修正済みと判断した。それは運だった：次の実行で再発し、しかも**すでに移行済み**のファイル内、検査を通過した**後**の fetch で起きた。実測された原因：失敗した URL は `http://127.0.0.1:5060`、`:5061`、`:6000`、`:6566` —— いずれも正当なポートであり、いずれも WHATWG fetch の**ブロックポート一覧**に載っている。`fetch` は何も待ち受けていなくても拒否する（`:5060` → `bad port`、`:54321` → `ECONNREFUSED`、許可されているが空）。`listen(0)` は「空いている任意のポート」を求めるので、時折その一つを引く。ストレス検査で決定的に再現：並列 400 台で 2 件、800 台でも 2 件。単独実行では通った理由も、三回の緑が何も証明しなかった理由も、これで説明がつく。`startServer` はブロックポートを引いたら引き直し、二十回で明確に失敗する（無限再試行はしない）。待機とアドレス検査は残す——説明は誤りだったが挙動は正しい。そして引き直しの分岐は、ブロックポートを引かないかもしれない検査ではなく、分岐を強制する偽装で検証する。それが前の版の誤りそのものだからである `v1.26.143`
- **誰も見ていない場所で壊れる収集** — 三つの発見、同じ形である：機械の上で異常が起きており、機械自身はそれを知っていて、自分のディスクにしか書いていない。**(1)** 一日中 Codex で作業する利用者の記録に、`claude-code` は二時間ごとに更新されているのに `codex` の行が一度も存在しない。それは正常に戻ったアダプタが作れる状態ではない。走査が到達するすべての結末は心拍を送る — 未導入も、新着なしも、読めなかった場合もである。行が残らない終わり方は三つしかない：例外、停止、設定による除外。そしてその三つはいずれも、そのマシンの `logs/scanner.log` に一行を書いて終わっていた。内容は正確で、置き場所は誰も読まない唯一の場所である。サーバから見ると「その道具を導入したことがない人」と区別がつかず、六週間そう読まれ続けた。現在は三つとも報告する：`adapter_error`（メッセージ付き）、`adapter_timeout`、`skipped_by_config`。停止の検出は新規である — 例外は捕まえて報告できるが、戻らない走査はループを占有し、後続のアダプタを飢えさせ、スケジューラが諦めた時点で全体が死ぬ。同じ利用者の記録がまさにその形である（最初の道具は最新、後続四つは七月のある日で停止）。メッセージは切り詰めて監査表へ送り、長さの決まった理由コード列には入れない。**(2)** 十五項目の自己点検のうち、データが実際に届いているかを問うのは `usage_roundtrip` だけであり、残り十四項目は導入済みかを問う。v1.26.81 はこれを `--quick` から外した。毎日全ローカル DB を走査するのは重すぎるという理由で、それ自体は妥当である。測られていなかったのは反対側だ：`--quick` は自動更新の経路であり、手で導入器を再実行しない人にとっては**唯一**の経路である。つまり「毎日はやらない」が「一度もやらない」になった。当該マシンは何週間も毎日十四項目を送り、十五番目を一度も走らせていない。現在は七日ごと。目印が読めない場合はすべて「実行する」側に倒す。**(3)** 同じ表を読んでいて見つけた：別の利用者の心拍五行はすべて当日のもので、版はすべて六月十八日の導入当日のものである。完全な更新は MCP の中にしかなく、MCP と対話する AI 道具を誰かが開いたときにだけ動いた。セッション開始フックは同期スクリプトだけを走らせる。それは修復であって前進ではない。彼女の使う編集器はその一覧にない。壊れてはいなかった — 八週間、そのマシンに更新を促す者がいなかっただけであり、その間に出したどの修正も彼女には届かない。更新は `shared/auto-update.js` に移り、二時間ごとの定期実行が担う：AI も、モデルも、判断も要らない。`v1.26.142`
- **言い方がたまたま一致したときだけ答えられる記憶** — 二件の報告、同じ根本原因：セッション開始時に渡される文脈はすべて「押し出し」である。何が既知かは述べるが、いつ調べに行くべきかを告げる一文がどこにもない。したがって利用者の言葉が一覧の見出しにたまたま衝突したときだけ機能し、それ以外では静かに失敗していた。**(1)** 文脈はチーム規範の一覧を `ownmind_get("standard_detail")` で締めくくっていたが、本番に対して実測すると、本文を自身のレコードに持つ規範——つまり最近書かれたもの、まさに問われている当のもの——に対して `{"data": []}` を返す。発見方法：同僚の端末がセッション開始時に受け取るものだけを与え、当該作業用のスキルは一切入れていない独立した二つのアシスタントに尋ねた。両方が見出しから規範を認識し、両方が先に読もうとし、両方がその呼び出しに手を伸ばし、そして**両方が自力でそれを怪しいと指摘した**。彼らは正しく、救ったのは彼ら自身の慎重さだけであり、設計上これを止めるものは何もなかった。**(2)** 「検索」という語は文脈に一度も現れない。見出しを並べ、名前をすでに知っているものを取り出す方法を二つ示すだけなので、「發 pages」は調べられ（見出しに一致する）、「公司 pages」は調べられない。同日に報告された、より重い例：本番サーバの接続情報が数週間前から記憶にあったのに、アシスタントは「kkvin.com の情報は持っていません」と答え、調べるよう言われた瞬間に見つけた。自信をもって述べられた偽の文であり、しかも説明はつく。文脈が描画するのは profile・鉄則・規範の見出しであって、**プロジェクト記憶は一件も含まれない**。座っている位置からは「情報を持っていない」と「情報が存在しない」が同一に見える。18 KB の文脈に `kkvin` は 0 回しか現れなかった。修正は三つの文である（文脈自体は増える。最小の検体で 621 バイト。ただし新たな一覧は追加しない）：一覧の規範は検索で見出しを行に解決してから読むこと。認識できない社内用語は、答える前に検索する理由であること（意図的に未知の語に限定した——「毎メッセージ検索」は雑音であり、雑音こそ無視される）。そして利用者自身のものについて、検索せずに「情報がない」と言わないこと。それは世界についてではなく**彼の記憶についての**主張だからである。次にレビューが配信の誤りを捉えた：初稿はその三文を運用マニュアルに置いたが、マニュアルは `instructions` に載り、初期化ルートはそれを `compact` が false のときしか送らない。そして `ownmind_init` を含むすべての呼び出し元が compact を要求するため、その半分は誰にも届いていなかった。ペイロードのうち唯一剥ぎ取られる部分に置かれており、テストは原文をテキストとして読むだけなので違いを見分けられなかった。いまは生き残る場所にある：すべての道具が毎ターン読む `ownmind_search` と `ownmind_get` のツール説明、および七つの `configs/*.md` 雛形である。レビューはさらに、描画された `[團隊] ` の接頭辞のせいで指示どおりの検索が何も返さないと報告したが、本番で実測すると当該規範が先頭行で返る。「公司 pages」も `kkvin.com` も同様である——丁寧で具体的、そして誤っていた推論として記録する。**続いて Vin が、残り半分を見つける問いを投げた：** インストール時にこれらは CLAUDE.md に書き込まれるべきではないのか。書かれるべきであり、書かれていなかった——`install.sh` は五行を一度書いたきり、ファイルに「OwnMind」の語があるという理由でその後は永久に飛ばし、更新スクリプトはこのファイルに一切触れていなかった。したがって各機の内容はインストール当日で凍結しており、三度更新された版をいまだに抱えた機械もあった。規則はいまや単一の原本にあり、四つのインストール／更新スクリプトが共有する一つの Node ヘルパーが、利用者自身の指示ファイルへマーカー付きブロックとして注入する。更新のたびに書き直され、マーカーの外は一文字も触らない。マーカーのない旧ブロックは、**その全行が本プロジェクトの出荷物と一致する場合にのみ**置き換え、そうでなければ残して一行知らせる——そのファイルは本人が自分の鉄則を書く場所だからである。二回目のレビューはさらに「着手する前に検索」を削った：コーディング用のアシスタントはほぼ毎ターン、ファイルを編集する。したがって毎ターン発火し、何も返さず、このブロック全体は無視してよいと教えてしまう。語彙ではなく指示対象を引き金にする形へ置き換えた。同じ回で三点を修正：アシスタントが持っていない ID を使えという訂正規則、400 文字のプレビューで記憶全体を上書きしうる更新経路、そして一人のアシスタントが全社の規範を書き換えるのを止めるものが何もないこと `v1.26.141`
- **どちらも OK と表示された二つの失敗** — 一つは Windows 利用者からの報告、もう一つはリリース確認中に見つけたものである。**(1)** `update.ps1` は途中で二度例外を出したうえで `[ OK ] Upgrade rules synced to detected AI tools` と表示していた。`Get-Content -Raw` は 0 バイトのファイルに対して `''` ではなく `$null` を返す。`[regex]::Replace()` は null 入力を受け付けないため代入自体が完了せず、次の行は null に対して `.TrimEnd()` を呼ぶ——これが二つ目の例外である。手を入れる前に pwsh で三通り再現した：**存在するが空のファイルは例外**、存在しないファイルと内容のあるファイルは正常。つまりその機械の六つの AI ツールのうち一つだけが黙って規則を受け取っておらず、まとめの行は固定文字列なので、そもそも別のことを言いようがなかった。書き込みは `scripts/windows/lib/append-upgrade-rule.ps1` に切り出して単体で動かせるようにし、null は本来の意図どおり空文字列として扱い、対象ごとに written / skipped を返し、失敗した対象は名指しし、まとめには実際の件数を出す。あわせて BOM なし UTF-8 で書くようにした——`Set-Content -Encoding UTF8` は PowerShell 5.1 で BOM を付け、これらのファイルは他社のツールが読むからである。**(2)** 整体分析の 14 日・30 日レポートが再び失敗しており、v1.26.137 の説明——「LLM ゲートウェイに 40 KiB の固定上限があり、二分探索で測った」——は誤りだった。当時の探査本文は短い一文の繰り返しで作っており、実際のレポートよりバイトあたりのトークンがはるかに少ない。したがって二分探索が見つけた境界はバイトの境界ではなかった：40,214 バイトの探査は通り、経路が実際に送る 35,301 バイトは拒否され、その同じ本文が二十分後に逐次三回・並行三回、計六回すべて成功した。**ゲートウェイはその瞬間の空き容量で拒否する。** 7 日レポートが無事だったのは小さいからにすぎず、それこそがこれをサイズ上限に見せていた。`callLLMSwitch` は 408・429・5xx と接続そのものの失敗を再試行する——三秒間隔で二回、全体 60 秒の期限つきで、各試行のタイムアウトもその期限で切り詰めるため、三回の停止で九十秒待たされることはない——それ以外の 4xx は一度で返す（408 は 4xx だが、これはゲートウェイ自身が再試行を求めるものである）。上流の応答は 200 文字ではなく 2,000 文字を残す。これは v1.26.137 が「未修正」として挙げた点そのもので、今回の原因究明にはサーバから手作業でリクエストを再送する必要があった。記録が、二番目と三番目のプロバイダが失敗した理由のちょうど手前で切れていたからである。**二回のレビューで、新しく書いたコード自身の問題が七つ見つかった。いずれも当初のテストからは見えなかった。** 「(after N attempts)」を付ける行は `lastError.message` への代入だったが、実際に中断された fetch が返すのは `DOMException` で、その `message` はゲッターのみである——モジュール（厳格モード）ではこの代入が例外になる。つまりタイムアウトのたびに記録に残るのは `TypeError: Cannot set property message` で、2,000 文字の上流の理由はまるごと失われる。その場面を担当するテストが緑だったのは、中断を `new Error()` に `name = 'AbortError'` を付けて自作していたからで、素の Error の message は書き込み可能である——**インタフェースの両端が偽物なら、テストは偽物どうしが一致することしか証明しない**。いまは signal を尊重する `fetchImpl` を使う。再試行の可否はエラーメッセージの部分一致で決めていたが、そのメッセージにはモデルの出力が埋め込まれている。ネットワークエラーに言及しただけのレポートが三回再試行されていた。判定は失敗が起きた地点へ移した。期限は失敗後・待機前にしか確認しておらず、待機で期限を越えたあとに残り一ミリ秒の試行が始まりえた。さらに、BOM なし UTF-8 で書きながら `Get-Content -Raw` で読むのは Windows PowerShell 5.1 では破壊のループである——BOM のないファイルはシステムコードページで復号される——両方の読み取りを `ReadAllText` にし、あわせて本変更より古い欠陥も直した：規則本文そのものが BOM なしの UTF-8 中文であり、**繁体字中国語版 Windows はこれまで、見つけたすべての AI ツールに文字化けした規則を書き込んでいた**。二回目のレビューでは、同じタイムアウトが別の入口から入ることが分かった：`res.json()` が分類の外にあり、ゲートウェイが 200 のヘッダを返してから本文の送出で停止すると、生の DOMException のまま再試行されずに抜け、記録には試行回数すら残らない。ほかに三件——試行間の待機が期限で切り詰められておらず「60 秒の期限」が実際には 63 秒だったこと、`retries: NaN` が `retries: -1` と同じ未定義参照に至ること、解析できない `apiBase` が同じ答えのために三回再試行されること。`update.sh` も件数を数えて表示するようにし、除去の失敗を同期成功として通していた `|| true` を外し、`cat` を単独で確認するようにした——コマンドグループが返すのは最後のコマンドの状態なので、読めないスニペットは空の規則ブロックを書いて成功と報告していた `v1.26.140`
- **テストを走らせるたびに赤が出て、しかも毎回違う赤だった** — `npm test` を四回連続で実行すると失敗数は 3、4、4、2 で、毎回違うテストが落ちた。どれも自分のファイル単体では通り、最も頻繁に落ちた四つのファイルは一緒に走らせると 43/43 で三回連続通過し、直列実行（`--test-concurrency=1`）で 4375 件を通すとコンソールのビルドを要する二件しか落ちない。つまりそれらのテストは一つも壊れていなかった。壊れていたのは共有していた作りである。32 のファイルがリクエストごとにサーバを開く（`app.listen(0)` → `address()` を読む → fetch → `close()`）実装で、テストランナーはファイルを並列に実行する。その負荷下では `address()` が常に使える値を返すとは限らず、誰も確認していないので、ポートが `undefined` のまま URL に入り、失敗は `[TypeError: fetch failed] { cause: Error: bad port }` として、スケジューラが当たったテストに現れた。四つのうち二つはさらに直接的で、`listen(0)` の次の行で同期的に `address()` を読み、'listening' イベントを待っていなかった。**帰属を特定できない赤は、本物の赤より悪い**。それが教える習慣は「赤ければ再実行」であり、それこそ本当の回帰が出荷される経路だからである。修正は共有ヘルパー：'listening' を待ってからアドレスを読み、使えないアドレスから URL を組まずに実際に見えた値を報告し、`listen` のエラーをタイムアウトまで待たずに浮上させ、閉じる際に生きた接続を切る。実際に落ちた四つのファイルだけを移行した——落ちたことのないファイルのために 32 ファイルを触るのは見合わない——そして戻ってしまわないようテストで固定した。検証は問題を見つけた方法と同じにした：現在は三回連続の完全実行が**同一の失敗**になる。これが「フレークが消えた」証拠であり、「今回は運良く緑だった」ではない。併せて二点：`update.sh` は `hooks/*.sh` しか同期せず、`~/.claude/hooks` 配下の `.js` を三バージョン古いまま残しながら「Hook scripts synced」と表示していた（削除ではなく同期にした。そこを指しているほど古いインストールは黙って壊れるからである）。そしてその隣の `cp … 2>/dev/null || true` は、モジュールのコピー失敗を成功として通していた `v1.26.139`
- **セッション開始時のメモリ読み込みが空になり、しかも `init: ok` と報告していた** — Windows で 1.26.137 に更新した直後に実測：`[OwnMind v?] Memory loaded`、鉄則ゼロ件、プロフィール行はコロンだけ。何も失敗していない——stderr は無言、終了コード 0、ログには成功イベント——読み込まれた内容が空だっただけである。原因は `~/.ownmind/cache/memories.json` に二つの書き込み者がいて、しかも構造が違ったこと：SessionStart フックは init レスポンスをそのまま保存し、MCP はメモリ種別ごとに配列を保存する。フックの条件付き同期の第二段は「キャッシュの sync_token がサーバと一致すれば `cache.data` をそのまま init ペイロードとして返す」ため、MCP の init が走ったあとはレンダラが種別キーのオブジェクトを受け取り、そこから `.profile` と `.iron_rules_digest` を読もうとしていた。**v1.26.133 までこの衝突を隠していたのは偶然である**：MCP は `account` フィールドを書いていなかったので、そのキャッシュは「帰属不明のファイルは他人のもの」という v1.26.82 の規則で拒否され、フックは黙って再ダウンロードしていた。MCP のキャッシュにスタンプを付けたこと自体は正しい——あるアカウントが別のアカウントのメモリを引き継ぐのを防ぐ——が、その偶然の保護を取り除いてしまった。修正は互いに独立した二箇所に入れた。どちらか一方だけでは戻り道が残るからである：MCP は自分専用のファイルを持ち、`readCache` は他の利用者向けの形状を拒否するので、すでに誤ったファイルを持つマシンは手で削除せずとも次のセッションで自己修復する。形状判定は意図的に否定形（単数の種別キー）にした。肯定形ではすべてのアカウントに必ず存在するフィールドを名指しする必要があり、ルールもプロフィールも無いアカウントにはそのようなフィールドがほとんど無い `v1.26.138`
- **インストーラが「API key updated」と表示する根拠は、ファイルではなく自身の意図だった** — 二つのインストールスクリプトを静的レビューして発見。まず良い知らせ：三つの鉄則それぞれの元となった欠陥はすべて修正済みである——「すでに設定済みなら鍵の書き込みを飛ばす」分岐（v1.26.91）、失敗を沈黙に変えていた消音（残っているものはすべてガードか明示的な既定値を持つ）、そして読めと言った当のログごと `~/.ownmind` を復元してしまうロールバック（v1.26.98、ログは復元範囲の外に移動）。残っていたのはより小さく、しかし同じ形だった。**(1)** 両スクリプトは設定ファイルを書いたあと、メモリ上の旧鍵と新鍵を比べて「API key updated」と表示していた——実行が意図したことであり、実際に着地した内容ではない。一方、同じファイルの三十行下では、同じ鍵が `~/.claude.json` へ書かれる際にファイルを読み戻して確認し、メッセージに `(verified by reading it back)` と明記し、コメントで当該ルールを名指ししている。一度の資格情報書き込みの二つの半分に、二つの証拠基準。しかも**未確認の側こそ、すべてのフックが鍵を読むファイル**であり、アカウント切り替えで変更しなければならない二箇所のうちの一つである。現在は読み戻し・解析・比較を行い、鍵がそこに無ければはっきりそう言う——沈黙もせず、致命的にもしない。**(2)** `ConvertTo-Json -Depth 10`。このファイルは利用者がフックを追加する前から五階層の深さがある。PowerShell では深さの超過は*警告*にすぎない：5.1 で `$ErrorActionPreference = 'Stop'` の下、四階層のオブジェクトに `-Depth 3` を適用すると `{"a":{"b":{"c":{"d":"System.Collections.Hashtable"}}}}` が書き出された。四つの書き込みすべてを cmdlet の上限である 100 にした。この二つが重なると完全な失敗の連鎖になる：切り詰められた書き込み、読み戻しなし、それでも「API key updated」と表示。**(3)** そして主張が証拠より先に来ていた——`OwnMind installation complete` が検査より上に表示されるため、`install.sh` は同じ実行の中でその直後に `[FAIL] Installation did not complete.` を出せた。両方のバナーを検査の後ろへ移し、セルフチェックを包んでいた空の `catch` も、検査自体が実行できなかったときに何か言うようにした `v1.26.134`
- **Windows でのみ発生し、いずれも自分では報告できない三つの欠陥** — 一台のマシン（Windows 10 Pro、Claude Code デスクトップ版、クライアント 1.26.132）で 19 個の MCP ツール、5 個の Claude Code フック、3 個の git フック、スケジュールされたスキャナ、9 本の PowerShell スクリプト、オフライン退避をすべて実行して発見した。**(1)** SessionStart フックは「前回のセッションで待機中のメッセージ」という見出しを表示し、**三つのストリームすべてを ignore する**ヘルパー経由で子プロセスに委譲し —— その子プロセスは各ブロックを自分の stderr に書く —— 次の行でスプールを空にしていた。実測：77 バイトのスプールが、見出し一行と 0 バイトのファイルになった。このスプールはこのプラットフォームでは稀な経路ではなく唯一の経路である：書き込む二つのプログラムはどちらもデスクトップ版でコンソールデバイスを開けないために退避してくるので、25 分のセッションで 19 件が蓄積し、次回起動時にすべて破棄された。現在はプロセス内で出力し、空にするのは実際に書き出せた場合のみ、そして空でないが何も読み取れないスプールは削除ではなく退避する。**(2)** `ownmind_init` は `compact=true` を要求し、compact な応答は `iron_rules` 配列ではなく整形済みのダイジェストを返す。したがって `data.iron_rules || []` は毎回の init で空配列をフィルタし、`[]` をルールキャッシュに書き込んでいた。このアカウントで唯一 `lint_validator` を持つルールは 20:25 にはキャッシュにあり、init を一度呼んだ 20:30 には消えていた。reply-lint の Stop フックはセッション中ずっとバリデータをゼロ個しか解決できず、混在率 67.3% の返信が無言で通過した。断続的に見えたのは pre-commit フックが空のキャッシュを見つけると再構築するからで、つまりそのルールはコミットの後だけ機能していた。同じ式はオフラインキャッシュのチーム標準・コーディング標準・プロジェクト・環境・ポートフォリオも空にしていた。**「無回答」は「空という回答」ではない**：応答が答えられなければ答えられるエンドポイントに問い、そちらも失敗すれば両方のキャッシュは現状を保つ。**(3)** `safe-spawn` は返す stdout のホームディレクトリを `~` に置換する。アップロードされる報告には正しく、比較には誤りである。スケジューラ検査は一つの文字列で両方をこなし、`"~\.ownmind\…"` と書かれたアクション一覧が絶対パスのインストール先を含むかを問うていた。含みうるはずがないので、**インストール先がホーム配下（既定ではすべて）である限り、完全に健全なマシンでもこの検査は必ず失敗した** —— 実測した環境のタスクは Ready、LastTaskResult 0x0 でありながら、壊れていないものの再登録を指示され、再登録しても翌日に同じ比較で落ちる。同じルールの PowerShell 側はタスクのアクションを直接読み `~` を見たことがないため、検査と修復がまた食い違っていた `v1.26.133`
- **インストールのために書かれた二つのルールが、まさにインストール中には発火できなかった** — トリガーの判定は四つのコマンド形状（`git commit/reset/rebase/merge`、`git push`、削除系、デプロイ系）しか認識せず、それ以外は認証情報を読む前に exit 0 していた。この環境での実測：`bash install.sh --api-key <k>` は出力ゼロ・終了コード 0。一方 IR-001 —— **「インストールスクリプトが成功と表示しても信用しない、自分で設定ファイルを開いて値を確認せよ」** という内容そのもののルール —— には `trigger:install`、`trigger:setup`、`trigger:config`、`trigger:api_key` が付いていた。タグは一度も間違っていない。**`install` という名前のトリガー区分が存在せず、そのタグを誰も問い合わせなかった**のである。インストールと鍵のローテーションはこのシステムで最もリスクが集中する操作の一つでありながら、唯一まったく門のない区分だった。同じ取得処理は一行に三重の消音も抱えていた —— `curl -sf --max-time 3 … 2>/dev/null` —— `-f` は 4xx/5xx のいずれでも空のボディを返し、空の結果は「該当するルールなし」とまったく同じ経路を通る。したがってサーバ停止、失効した鍵、本当に該当ルールがない場合の三つが、フックにとっても利用者にとっても区別できなかった。**一言も発せずに自分を無効化できる安全機構は、存在しない安全機構より悪い。存在しないことは少なくとも目に見えるからだ。** 失敗は HTTP ステータスまたは curl の終了コードとともにアクティビティログへ記録されるようになり、stdout には決して出さない —— そこは正しい封筒であり続ける必要がある。これは v1.26.131 と同じ形をしている：**このファイル自身の別の箇所のコメント**が、失敗を沈黙に変えるなと既に書いていた（v1.26.120）のに、最も重要な一つの呼び出しだけが変更されずに残っていた。もう一つ、全体を繰り返すところだった境界条件がある：`\bAPI[_-]?KEY\b` は `OWNMIND_API_KEY` にマッチしない。アンダースコアは単語構成文字なので `D` と `A` の間に `\b` は存在しないからだ —— 接頭辞付きの形こそ実在する環境変数すべての姿であり、そのパターンが唯一見えなかった形が、唯一現れる形だった `v1.26.132`
- **ブロックは正しく、そこに書かれた名前が誤っていたため、利用者は持っていないキーを探しに行かされた** — Anthropic と OpenAI はどちらも API キーの接頭辞に `sk-` を使い、`openai_api_key` という一つの正規表現がその一族全体を所有していた。v1.26.124 をコミットする最中に実地で観測された —— そのリリースが追加した既定スキャンが、そのリリース自身をブロックした：`value 符合 openai_api_key 格式 (detected_by=regex:openai_api_key) matched="sk-ant-a…AAAA"`。同じ一文の中で、マスクされた断片は `sk-ant-` と言い、ルール名は OpenAI と言っている。コミットを止めるのは仕事の半分であり、利用者が実際に行動の根拠にするのは名前のほうだ —— これは、隠れた一致によって bug id=6 が誤診されたのを受けて `matched_text` を追加した v1.26.28 と同じ失敗である。Anthropic のキーは自前のルールを持つようになり、ループが最初の一致で返るため OpenAI より前に配置した。さらに OpenAI 側のパターンも独立に `ant-` を拒否するので、並び替えも片方のパターンの編集も、それらを黙って誤った提供元に返すことはできない。除外するのは `ant-` という四文字であって `ant` の三文字ではない。ゆえに `sk-antelope…` は今も OpenAI である —— `(?!ant)` と書けば、誰も気づかない方向へ誤って分類される。Anthropic のキーを持たない利用者には、見つからないという経験すら起こらないからだ。既存の `regex:openai_api_key` に対する表明はすべて先に確認し、いずれも本当に OpenAI である `sk-proj-…` を使っていたので、この分割は誰の足場も動かしていない。新しい検体は実行時に組み立てられ、単一のリテラルへ「整理」しないよう注記が添えられている —— リテラルのキーは、そのテストファイル自身を、まさにそれが検証するスキャナによってコミット不能にする。前のリリースで `pre-commit-secret-baseline.test.js` に起きたことそのものである `v1.26.125`
- **四つの防御線のうち二つは一度も何も止めていなかった。しかも原因は同じだった** — インストール・設定・データ送信・鉄則の発火・更新を、テストを読むのではなく実際に走らせて Windows 実機で検証した。使い捨てリポジトリに整った形式の AWS キーと Anthropic キーを staged してコミットしたところ、**そのまま通った**。出力なし、exit 0。連鎖させたプローブフックにより wrapper もフックも動いていたことが確認できたので設定の不備ではない —— 秘密情報スキャンは鉄則ごとのループの内側、`isSecretGuardRule` の背後にあり、そのループの手前には「機械検証可能なコミット鉄則を持たない利用者」が必ず通る出口が二つあった。このアカウントの鉄則三つはすべてリマインダーのみで、それは**新規アカウントの既定状態**である。ゆえに両方の出口が発火し、宣伝されていたコミットの強制ブロックは、検証ブロックを手書きしたことのない全員にとって空だった。スキャンは今やすべての鉄則の出口より前で走る。漏れた資格情報は好みの問題ではないからだ。ルール駆動の経路は変えていない（secret-guard 鉄則があれば baseline は退き、同じ漏洩を二度印字しない）。`OWNMIND_BYPASS=all` は監査行を書いた上で通す —— 何をしても切れない検査は `--no-verify` を強い、それはすべてを一度に無効化し何も記録しない。同じ病巣が返信リントも無効化していた：`iron_rules.json` は検証エンジン用に作られ、v1.21.0 が `lint_validator` を探す二人目の消費者を同じファイルに向けたが、フィルタは広げられなかった。結果、バリデータだけを持つ鉄則は取得された後、ディスクへ書く途中で捨てられていた。実測：2 バイトのキャッシュ、解決されたバリデータ 0、エンジンが 79.3% の混在と判定する返信に対して「違反なし」—— きれいな返信と見分けがつかない。述語は `shared/cacheable-rules.js` に移し、このファイルの書き手二つが共有する。「設定しなければ検査しない」という全体既定は v1.26.13 が理由あって定めたものなので**意図的に変更していない**。同じ回でさらに三つ：Node フックが「今日」を UTC で計算する一方、二つのシェルフックと MCP は現地日付を使っていたため、UTC+8 のマシンでは登録済みの二つの SessionStart フックが毎日 0 時から 8 時まで互いの更新マーカーを覆し、その日のログを二つのファイルに割っていた —— 現地時刻が UTC と異なる場所でしか再現せず、それは CI では決して起きない。`install.sh` には `msys*` の分岐が存在せず、`bootstrap.sh` 経由の Windows インストールはすべて使用量スキャナを一切スケジュールせず、警告を一行だけ出していた。それは隣のコメント自身が記述している死に方そのものである。そして自己診断はその存在しないスケジュールを `[ OK ]` と認めていた。`Get-ScheduledTask` はマシン全体で共有され、別のインストールに属するタスクを見つけていたからだ —— つまり一度でも OwnMind を入れたマシンは、その検査に永遠に合格し続ける `v1.26.124`
- **中途半端にインストールされたマシンでは、鉄則チェックがまったく動いておらず、しかも何も言わなかった** — シェルのフックは `~/.claude/settings.json` から API キーを読むために、パスを `node -e` のソースへ差し込んでいた。これは `to_win_path` が `cygpath -m` のスラッシュ出力を返す限りにおいてのみ安全であり、**`path-helpers.sh` が無いときのフォールバックは `$HOME` の中身をそのまま返す**。Windows ではそれはバックスラッシュのパスなので、`C:\Users\...` はエスケープ列として JS パーサに届き、読み取りは例外を投げ、catch は空のキーを出力し、フックは一言もなく exit 0 した。v1.26.94 と v1.26.112 と同じ系統であり、その周りの `2>/dev/null` こそ利用者自身の鉄則が名指しする赤旗である。失敗を沈黙に変えるからだ。パスは現在 **argv** で渡される。バックスラッシュも空白もアポストロフィも通る形であり、`path-helpers.sh` のヘッダ自身が勧めながらここでは採用されていなかった。資格情報の読み取りも stderr を捨てなくなった。**最も効いた変更はテスト側にある**：フックの stderr を捨て、失敗メッセージは推測（「コマンドが抽出されなかった」）だった。真偽値の `reached: false` は、解析できない payload も、上流のガードによる先行終了も、クラッシュも同じ見た目になる —— そしてその三つはそれぞれ異なる stderr を書く。終了コード・stdout・stderr を添えたところ、真の原因は 2 分で分かった。物差しがもう二つ直った：`chmod 0o500` は NTFS で書き込み不可のディレクトリを作れないため、「劣化は告知される」テストは検証対象の劣化そのものを発生させていなかった（通常ファイルをディレクトリの位置に置けば、どの環境でも失敗する）。そして `finally` の裸の `rmSync` は Windows で EPERM を投げた —— その一時ディレクトリはフック自身が起動した更新の子プロセスがまだ掴んでいた —— **アサーションがすべて通過した後で**、無関係な名前のもとにテストを失敗させていた `v1.26.120`
- **Windows の赤いテストを追ったら本物の不具合が出た：記憶ファイルは一度も書けていなかった** — この課題は「Windows でだけ失敗するアサーション」の一覧で、すべて物差しの側が壊れていると見なされていた。1 つは違った。`projectSlugFromPath` はプロジェクトパスのスラッシュだけを置換していたが、Windows のプロジェクトパスは `C:\` で始まる —— **コロンはそこではディレクトリ名に入れられない**。NTFS が代替データストリームの区切りとして読むからだ。よって `mkdir` は失敗し（実測は ENOENT。Windows がまた別の形をその一つの errno に潰している）、書き込みは飲み込まれ、SessionStart フックは **Windows で記憶ファイルを一つも書けていなかった**。呼び出し側が v1.26.83 で加えたコメントは、まさにそれを防ぐためだと述べている（「これがないと AI は最後に書かれたスナップショットを読む。Windows ではそれが一度も起きていない」）。あのリリースは経路上のすべてを直し、ディレクトリ名を組み立てるその一行だけを見落とした。現在はスラグがコロンも落とし、`C--Users-Vin-X` を生成する。これは Claude Code 自身がプロジェクトディレクトリに使う綴りでもあるので、ファイルは AI が既に見ている場所に落ちる。残りは本当に物差しの問題だった：リポジトリ相対パスを POSIX のリテラルと比較していたもの（`src\app.js` 対 `src/app.js` —— 正しいファイルを見つけ、区切り文字で外した）、`/abs/path` と綴られた偽のホームを `path.join` がバックスラッシュに変える一方で、検証対象の性質（裸のファイル名ではなく絶対パス）は最初から満たされていたもの、そして通常ファイルをディレクトリに見立てると「Windows を含むあらゆるプラットフォームで」ENOTDIR になるというコメント —— 実測ではそこは ENOENT であり、不正文字も予約デバイス名も長すぎるパスも同様だった。つまり OS 自身の答えが「存在しない」であり、製品は正しかった。その最後の規則（ENOENT だけが未インストールを意味する）は、実際の OS エラーを捻出せず注入したエラーで検証できるようになり、その errno を作れる環境だけでなくすべてのプラットフォームで成立する `v1.26.119`
- **Windows では決して通り得ないアサーション、しかも告発された 2 つのファイルは健全だった** — `chmod` は NTFS では何もしない。修正したマシンでの実測では、755 に設定したファイルは 666 として読み戻される。したがって `statSync(f).mode & 0o100` は、`scripts/bootstrap.sh` と `scripts/run-migrations.sh` の実際の状態に関わらず、その環境で両者を壊れていると報告していた —— 壊れていたのは物差しであり、測られた側ではない。Windows でスキップするのはより悪い修正だ。そのプラットフォームに答えが一つも残らなくなる。このリストで何ヶ月も生き延びた不具合は、すべてその形をしている。現在は代わりに **git のインデックス**（`100755`）に問う。これはどのプラットフォームでも本来より適切な問いである。ローカルのビットは「このマシンが今たまたまどうなっているか」を語るが、インデックスのモードは**クローンする全員が何を受け取るか**を決める。`100644` でコミットされたスクリプトは、作者の作業コピーがどうであれ、その全員にとって実行不能である。POSIX のビットは意味のある場所で引き続き検証する。インデックスに問えない事例が一つある —— インストーラのフック複製関数が一時ディレクトリに作る新しいコピーだ。そこでは Windows で答えられる半分、すなわちその関数が実行ビットを要求しているかを検証し、本当の観測はそれが可能な環境に残す。新しいヘルパーは自前の逆対照を持つ：`100644` でコミットされたファイルは必ず失敗しなければならない。さもなければ「Windows で通る」と「何を渡しても通る」は同じテストになる `v1.26.118`
- **自己診断は MCP サーバーが「登録されている」ことは確認したが、「起動する」ことは一度も確認していなかった** — `mcp_registered`（v1.26.112）は `~/.claude.json` を読み、`mcpServers.ownmind` の存在を確認する。それはあのリリースに必要だったものそのものだ。きっかけとなったマシンでは、そのキー自体が存在しなかったのである。見えないのは、**エントリはあるのに起動しない**場合だ。そしてこのリポジトリは、その区間を四度壊している。いずれも設定ファイルは完璧に読める：`cygpath -w` のパスが `node -e` のソースへ埋め込まれ、JS パーサがバックスラッシュを食べた `v1.26.94`。PowerShell 5.1 が同じソースから二重引用符を剥ぎ取り、しかも PowerShell 自身のパーサはそれを完璧だと述べた `v1.26.112`。Git Bash が cmd.exe の `/c` を `C:/` に書き換え、その書き換えられた形が起動コマンドに入り込む `v1.26.112`。そして `start.cmd` の「node が PATH にない」フォールバックの階段 —— その失敗はエラースプール内のファイルであって、設定ファイルの誤りではない `v1.17.77/79`。新しい `mcp_launches` は、Claude Code がセッション開始時に行うことを行う。そのファイルにある `command`・`args`・`env` を**一字も変えずに**spawn し、JSON-RPC のハンドシェイクを完了させ、`ownmind_*` ツールを数える —— 実装したマシンでウォーム 693 ミリ秒である。インストーラの意図からコマンドを再構成すれば、試されるのは意図だ。四度間違っていたのはディスク上のその一行である。**意図的に fail-open する**：タイムアウトは*判定不能*を意味し、決して*故障*を意味しない。冷えたマシンや負荷の高いマシンは、健全なままどんな予算も超えうるし、自己診断が走るのはインストール直後、最も忙しい瞬間である。v1.26.106 は「遅い」を「壊れている」と読んだ検査から、すでに一度捏造された FAIL を送信している。これは v1.26.113 の `lock_age_seconds` と正反対であり、違いは答えが何のためのものかにある。あちらは**振る舞い**を左右し、誤読は何かを壊す。こちらは**診断**であり、誤読は問題のないマシンを告発する。二つに分けたことで安価な確実性も残る —— `mcp_registered` は常に答えを返す純粋なファイル読み取りのままだ。もう一つ塞ぐべき副作用があった。サーバーは起動時にハートビートを送り、`collector-silence` はそのハートビートでマシンが沈黙したかを判断する。したがって毎日無人でサーバーを起動する検査は、誰も使っていないマシンを保証し続けることになる —— 検査対象を密かに保証する検査は、検査がないより悪い。診断としての起動には印が付き、ハートビートは送られない。すべてのテストは一時ディレクトリの偽サーバーに対してどのプラットフォームでも走る。それが要点である：上記四つの不具合が生き延びたのは、到達しうるテストがすべて動作する Windows マシンを必要としていたからだ `v1.26.117`
- **ハングしたテスト実行は、どこで止まっているかを何も語らない** — CI 導入の翌日、`main` の macOS レグは開始 20 秒で最後の行を出したきり 20 分間沈黙し、ジョブ自身の制限時間に打ち切られた。GitHub はこれを `cancelled` として記録するため、誰かがボタンを押したように見える。同じコミットの再実行は 74 秒で通り、調べる材料は何も残らなかった。同じ日の早い時間にも別のコミットで同じことが起きていたが、新しい push に打ち切られたため「ハング」として認識すらされていない。`node --test` には既定の制限時間がなく、しかもファイルを**投入順に**報告するため、終わらないファイル 1 つがその後ろのファイルすべてを黙らせる。現在ランナーは `--test-timeout=300000` を伴う。スイート全体が ubuntu で 30 秒、macOS で 47 秒なので 5 分は十分に遠く、超えたものは報告に名前が出て、20 分の沈黙ではなく数分で赤になる。**これはハングを直したものではない** —— ローカルでの全体実行 5 回でも、停止位置が指すファイルの 120 回実行でも再現せず、完全なログが残った唯一の回も再実行では通った。次に起きたときに名前が残るようにしただけである。網には穴もあり、それは推測ではなく実測である：node 20 では「決して解決しないテスト」は自力で失敗し「ハンドルを閉じないファイル」は捕まるが、node 24 では逆で、後者は依然として無制限のままである `v1.26.114`
- **スタブの `stat` では「このマシンでヘルパーが動くか」を問えない** — v1.26.111 は `PATH` 上のスタブ `stat` で方言の扱いを全プラットフォームに固定した。これは最も重要な性質であり、それらのテストが存在する理由でもある。しかし四つの事柄がその射程外に残っていた。いずれもこの欠陥が隠れ得た、あるいは再発し得た経路である。スタブはテストが選んだ方言しか答えないため、**このマシン**に問うものが一つもなかった。それこそ Linux で九リリースにわたり「否」と答えられ続け、その間 macOS ではすべて緑だった問いである。スタブのテストはヘルパーを直接呼ぶため、利用者に実際に噛みついた帰結も覆われていなかった。すなわち `acquire_update_lock` が空の経過時間を「判断できない」と読み、staleness の分岐を丸ごと飛ばし、中断された実行が残したロックが決して引き継がれない、という帰結である。第三に、どちらの形式も答えられないときヘルパーは**黙って**非ゼロを返していた。沈黙こそがこれを九リリース生き延びさせたものであり、二つの `2>/dev/null` が苦情を覆い、どの層も一言も発しなかった。現在はファイル名と試した両形式を stderr に述べる。呼び出し側は従来どおり fail-closed であり、方言のロジックには一文字も触れていない。第四に、ファイルが存在しない場合は日常である（`acquire_update_lock` は毎セッション、通常は存在しない `.reclaim` の経過時間を問う）。この経路は静かでなければならず、さもなければ先の診断が全利用者の起動時の雑音になる。ヘルパーを戻すと四つのうち三つが赤くなる。四つ目は設計上どちらでも緑であり、過剰修正を防ぐ番人である `v1.26.113`
- **MCP サーバーは Claude Code が読むファイルに一度も登録されていなかった** — Claude Code は `~/.claude.json` から MCP サーバーを起動するが、どのインストーラも `~/.claude/settings.json` しか書いていなかった。したがって `ownmind_*` ツールは誰のセッションにも存在しなかった。壊れていたのでも機能低下でもなく、登録されていなかったのである。これが隠れ続けたのは**見える側が動いていたから**だ。SessionStart フックは `settings.json` に設定され、Claude Code はフックについてはそのファイルを読む。フックは自前で HTTP 呼び出しを行うため記憶は文脈に現れ、誰ももう半分の所在を問わなかった。2026-08-09 の実測では、`settings.json` に完全かつ正しい `mcpServers.ownmind` があり、`.claude.json` には `mcpServers` キーすら存在せず、セッションに `ownmind_*` ツールは一つもなかった。一方で `node ~/.ownmind/mcp/index.js` を手で叩くと `initialize` は正しく応答した。リポジトリは半分は知っていた。`resolve-credentials.cjs` は両方を探し、v1.26.93/94 の記述も `~/.claude.json` を「何も書かないファイル」と呼んでいる。ただ誰も文を締めくくらなかった。何も書かないなら、登録されていないということである。**難しかったのは更新スクリプトの側だ**。インストーラを再実行する人はいない。自動更新の経路は `git pull` → `npm install` → `update.sh` であり、インストーラだけを直せば、このリリースは修復を宣言しながら既存のすべてのマシンをそのまま壊れたまま残すことになる。v1.26.104 が git フックのラッパーで陥ったのと同じ罠である。実装中に同じ病が二度再現し、いずれも元の欠陥が決して問わなかった同じ問い、すなわち**どのファイルか**によって捕捉された。ヘルパーは `os.homedir()`（Windows では `USERPROFILE`）を既定にしていたが、インストーラは bash の `$HOME` を使っていた。更新スクリプトは一方のホームから資格情報を読み、別のホームへ書いていた。どちらも正しく書き、正しく読み戻し、成功を報告した。誰も見ていない場所で。現在ホームディレクトリは明示的な引数であり、絶対パスでないもの、Windows での POSIX 形式は拒否される。さらに `mcp_registered` という自己診断がそのファイル自体に問い合わせる。`mcp_files` も `mcp_node_modules` も、ツールが存在しないマシンで緑だったからである `v1.26.112`
- **`stat -f` は BSD では書式文字列、GNU では `--file-system` を意味する。そのため更新ロックは Linux で自分の経過時間を計算できなかった** — CI の初日に見つかった。セッションフックは `stat -f %m … || stat -c %Y …` でファイルの経過時間を求めており、前者が**静かに**失敗すると仮定していた。Linux では 5 行のファイルシステム情報を **stdout** に出力してから非ゼロで終了するため、`||` が正しい epoch をその下に追記し、次の行の算術がそこで落ちる。すぐ上のコメントは 2 つのプラットフォームの違いを明記していたのに、その下のコードは片方が黙って失敗すると仮定していた。`2>/dev/null` が覆っていたのは、最初から stderr だけである。現在は 2 つの形式を別々に試し、結果が数字だけかを検査する。テストは PATH に置いた スタブの `stat` で駆動するので macOS でも走る —— 要点はまさにそこで、これは macOS の開発者には見えない不具合である。alpine コンテナで確認し、Linux の node 20 / node 24 でも全テストが通る。**これは上の Windows 系リリースの鏡像**である：Mac で開発し、別の場所で壊れる。これを直したことで、期限切れロックの引き継ぎ経路が Linux で初めて到達可能になり、そこに元からあった競合が表に出た：強制終了した引き継ぎ役が残したマーカーを片付ける際、脇へ退避したものが本当に計測した期限切れのマーカーかを確認していなかった。その間に作られた新しいマーカーを代わりに奪ってしまい、2 つのプロセスが同時に更新に入りうる。両方の実装で塞ぎ、それぞれ逆向きの検証も付けた。**残る窓は 1 つあり、覆い隠さず書き残してある** —— フック 8 本が競合する状況で、1 CPU では 60 回に 1〜2 回まだ赤くなる。追跡すると 4 つのプロセスが順に、いずれも**規則どおりに**引き継ぎ役になっている。それは上記の窓ではなくプロトコルの形そのものであり、プロトコル自身が「削除と再作成はアトミックにできない」と述べている `v1.26.111`
- **このリポジトリに CI が入り、初日に「どこでも走っていなかったテスト」を 3 つ見つけた** — それまで `.github/` にあるのは `CODEOWNERS` だけで、テストは誰かが手で走らせたところでしか走らず、実際にはそれは 1 台の macOS だった。**Mac では、Windows でしか壊れないテストは「失敗したテスト」ではなく「見えないテスト」である。** 前リリース分の不具合はすべてそうやって生き延びていた。必須ゲートは ubuntu × node 20、ubuntu × node 24、macOS × node 20。Windows は実行して結果を出すがマージは止めない —— 利用者は `main` から直接更新を取得するのでマージは即出荷であり、既存の 109 件の赤で出荷を人質に取ることはできない。`continue-on-error` を付けた matrix leg ではなく独立した job にしてある：そうした leg は `needs.<job>.result` では依然 `failure` を返すため、その値を読むゲートは「Windows だけが赤」と「全部が赤」を区別できない。初日に判明したこと：`install-failed-beacon-ps1` は**書かれて以来どこでも通っていなかった**。ハーネスが `function Fail` だけを抜き出していたが、`Fail` はファイルのさらに下で定義された関数を文字列補間しており、PowerShell は引数の組み立て段階で例外を投げ、検証対象の呼び出しに到達する前に `Fail` 自身の `catch { }` に飲み込まれていた。しかも探していたのは `pwsh` だけで、macOS には存在せず Windows では `powershell` と呼ぶため、両方でスキップされていた —— **そしてスキップは、サマリー上でパスとまったく同じに見える。** `scanner-schedule-repair` の 2 件は macOS 専用の `plutil` を呼んでおり、「macOS：…」は実質「どこでもない」を意味していた。現在は生成された XML を直接検証し、プラットフォームのツールがある場合はクロスチェックとして併用する。`v1.26.107`
- **Windows でしか壊れず、Mac からは見えない 4 つの不具合** — このリポジトリに CI はない。`.github/` にあるのは `CODEOWNERS` だけで、テストは誰かが手で走らせたマシンでしか走らない。そして Windows 専用の経路は、Mac ではスキップされるか、そもそも到達しない。**macOS で緑になるテストは、macOS の開発者にとって「通ったテスト」ではなく「見えないテスト」である。** **(1)** サーバーへ送られる診断ログが文字化けし、NUL バイトを 148 個含んでいた。`install.ps1` は `Tee-Object -FilePath` で書いていたが、Windows PowerShell 5.1 の `Tee-Object` には `-Encoding` パラメータが**存在しない**（PS 6 で追加）ため必ず UTF-16LE で書き、読む側は UTF-8 として読んでいた。該当機の `register-task-*.log` はすべて `fffe` で始まり、最も古いものは 2026-05-09 だった。**これは v1.17.83 の障害の材料そのものである**：NUL が 1 つあるだけで Postgres は JSONB 文書全体を拒否し、INSERT が失敗し、クライアントの再送キューが同じ行を送り続ける。当時はサーバー側で INSERT 前に除去して直したが、今回はその残り半分にあたる。現在は**推測ではなく BOM に従って復号する**。なお最初の修正自体が間違っており、それをテストが捕まえた：`JSON.stringify` は NUL をエスケープ列に変えるため、直列化後の文字列から生の NUL を探しても見つからず成功と報告してしまう。Postgres が拒むのは、まさにそのエスケープ列である。**(2)** スケジューラ検査が偽の FAIL を送っていた。`Get-ScheduledTask` は CIM コマンドレットで、モジュールの自動読み込みと CIM セッションの確立が先に必要になる。アイドル状態の Windows 10 で実測約 1.5 秒、予算は 5 秒 —— 3 倍の余裕だが、self-check が走るのはインストールや更新の直後、マシンが最も忙しい瞬間である。タイムアウトを 600ms に下げると、当時の記録と 1 文字違わぬエラーが再現した。`launchctl list` は約 20ms なので、同じ定数は macOS では 250 倍の余裕になる。この予算は、この呼び出しのために決められたものではなかった。さらにその下で、`safeSpawn` が返す `killed`／`signal` を呼び出し側が捨てて `error` だけを見ていたため、タイムアウトが利用者には「Windows と PowerShell が必要です」と表示されていた —— 両方あることが明白なマシンに対して。**(3)** 4 つのテストファイルが Windows では何も検査していなかった。`new URL(...).pathname` は Windows で `/C:/Users/...` を返し、node がそれを現在のドライブのルートに連結するため、`install-artifacts.test.js` は `MODULE_NOT_FOUND: C:\C:\Users\...` で**最初のアサーションの前にプロセスごと落ちて**おり、他の 3 つは ENOENT だった。macOS ではその pathname がたまたま妥当な絶対パスになるので、4 つとも緑だった。**(4)** Git Bash 検出器を実際に実行する唯一のテストは、どのプラットフォームでも通り得なかった。PowerShell を `-ExecutionPolicy Bypass` なしで起動していたためである。ポリシーを設定したことのない Windows クライアントの既定は Restricted で、`.ps1` のドットソースはそこで拒否される。macOS には PowerShell がないので、ブロックごとスキップされていた。出荷される呼び出し側はすべてこのフラグを渡しており、渡していなかったのはテストだけだった。直して初めて第二の層が見えた：`.cmd` スタブが cmd.exe のメタ文字をエスケープしておらず、実際の `bash --version` は 3 行目の末尾が `<http://gnu.org/licenses/gpl.html>` で終わる。cmd はこの `<` を入力リダイレクトとして読むため、Git Bash の 2 例とも否と判定され、テストは正常な検出器を告発していた。**新しいテスト 2 本はどこでも走る**。それが要点で、これらの不具合が生き延びたのは、到達しうるテストがすべて動作する Windows マシンを必要としていたからである。その後のレビューで、この検査自体が、まさに取り除こうとしていた死角を抱えていたことが分かった —— 走査対象が `tests/` だけで、同じ裸の `.pathname` が出荷コードにも生きていた。1 つ前のリリースで追加されたコミットメッセージ用フックである。Windows ではそれ自身の import が解決できず、しかもこのフックは設計上どんな異常でも exit 0 する —— つまり**コミットメッセージの規則は、そこで静かに一切効いていなかった**。現在は `hooks/`・`mcp/`・`scripts/`・`shared/`・`src/` も走査し、PowerShell を使うファイルの選別も大文字小文字を区別しない。以前の条件は小文字で引用符付きの `powershell` を要求していたため、この規則そのものに違反していたファイルを見落としていた。もう 1 点、NUL のエスケープ除去は、先頭のバックスラッシュ自体がエスケープされている場合でも、その 6 文字を見つけ次第削っていた。その 6 文字を本文として含む payload は、もはや解析できない文書になる —— サーバーが拒否し、キューが再送する。まさにそれが防ぐはずだったループである `v1.26.106`
- **同じ設定ファイルの中で、片方のアイアンルールフックは動き、もう片方は何か月も死んでいた** — v1.26.102 に更新した Windows の `~/.claude/settings.json` に、同じフックを指す PreToolUse が 2 件。`Edit|Write|MultiEdit|NotebookEdit` の方は exit 0、`Bash` の方は Bash コマンドを打つたびに `ERR_MODULE_NOT_FOUND` で exit 1。**2 つのファイルはバイト単位で同一**で、違うのは置き場所だけ：`~/.claude/hooks` に置かれた方は `../shared/helpers.js` を import するが、`~/.claude/shared/` はどのインストーラも作らないディレクトリなので、node は payload の 1 バイト目を読む前に終了する。フックはもともと静かなものなので、コマンドのたびにアイアンルール検査が見えない場所で死んでいても誰も気づかない。v1.26.92 はこの経路が壊れていることを既に把握し、新規インストールはチェックアウト側を指すようにしていた。しかしその判定は「この matcher の下に `ownmind-iron-rule-check` に言及する command があるか」であり、壊れている方も**言及していた**。壊れたコマンドが自分自身の修復条件を満たしてしまうため、書き換えられることは決してない。v1.26.92 が届いたのは新規インストールだけで、アップグレード組こそが全人口である。同じロジックの写しが install.ps1 / install.sh / update.ps1 / update.sh の 4 か所にあり、CI から届くのは bash の 1 つだけ —— 腐ったのは、まさにテストが触れない側だった。今は 4 つとも共有ヘルパーを呼び、**コマンドが違えば書き換える**。「存在すること」を「正しいこと」として扱わない。インストール自己診断も、どこかに写しがあるかを尋ねるのではなく settings.json に実際に登録されたコマンドを読む。ただしこのマシンでは、コマンドを読むだけでも足りない —— 同じ教訓のより鋭い形である。2 つの写しはバイト単位で同一なので、登録されたファイルは**存在する**。欠けていたのは、そのファイルが 1 行目で import するモジュールのほうだ。解決できない import は payload が動き出す前に node を終了させる。**存在することは、起動することではない。**そこで両方を検査するようにした。6/6 を報告しながらフックが死んでいる状態は、これで成立しなくなる。もう 1 点、レビューで見つかったもの：`install.sh` はヘルパーを裸の `VAR=$(...)` で呼んでいた。`set -eE` の下ではヘルパーの終了コードがそのまま伝播するため、読めない設定ファイルが 1 つあるだけで インストーラはその行で、しかも静かに中断し、自己診断を含む以降のすべてが走らなくなる。さらに、更新スクリプトの自己診断は修復の**後**に移した。先に走ると、同じ実行がこれから直す状態を報告してしまう —— マシン 1 台につき 1 件の警報が上がり、誰かが読む頃にはそのマシンはもう健全になっている。あわせて `.gitignore` に、インストーラがチェックアウトへ書き込む実行時ファイル 6 件を追加した。元の一覧は `.update-lock*` で止まっていたため、実機では毎回のアップグレードがツリーを dirty と判定して `git reset --hard` の分岐へ進み、その警告が毎回の雑音になっていた —— 利用者が実際に手を入れた変更は、そこで一緒に流れていく `v1.26.105`
- **コミットメッセージの検査は、前回のコミットのメッセージを読んでいた** — git が `.git/COMMIT_EDITMSG` に書き込むのは pre-commit が走った**後**である。したがって pre-commit がそのファイルを読むと、前回のコミット、あるいは前回中断した試行が残した内容を見ることになる。各段階の内容を出力するフックで実測すると、pre-commit は `FIRST MESSAGE` を、commit-msg は `SECOND MESSAGE` を見ていた。発見は手痛い形で訪れた。禁止されたトレーラーを理由にコミットが拒否され、それを削除して再送したところ、メッセージにもはや存在しない文字列を引用して再び拒否されたのである。**静かな方向のほうが深刻だ**。一度目の試行がきれいなら、きれいなテキストが残るので、二度目に持ち込まれた違反は素通りし、誰も気づかない。これが生き延びたのは、`GIT_COMMIT_MSG` 環境変数へのフォールバックがファイル読み取りの例外時にのみ発火するからである。まっさらなテストリポジトリでは必ずそうなるため、テストは本番が決して通らない経路だけを検証していた。メッセージ規則は commit-msg フックへ移った。git が本物のメッセージを渡すのはそこだけである。pre-commit はそれらを「合格」と評価させるのではなく**除外**する。検査していない規則を `all N rules passed ✓` に数えることは、何も言わないより強い主張だからだ。規則の選別は条件の型で行い、規則コードでは決して行わない。誰がどう採番していても機能する。ハードコードされた `Co-Authored-By:` の防護は**意図的に残した** — 最初の版ではこれを削除し、穴が空いた。規則エンジンの部分一致は大文字小文字を区別するため、git 自身の綴りである `Co-authored-by:` を取り逃がす。それを捕まえたのは、一文字も変えていない既存のテストだった。続くレビューは、このリリースを保護の**純減**に変えかねなかった点を突き止めた。`~/.ownmind` は git のチェックアウトそのものなので、pull が届いた瞬間にフックのロジックは入れ替わる。しかしインストール済みのラッパーはコピーであり、自動更新の経路はそれを一度も更新していなかった。つまり利用者は、メッセージを検査しなくなった新しい pre-commit と、新スクリプトを決して呼ばない古い commit-msg を同時に抱えることになり、唯一まだ動いている規則がその沈黙を覆い隠す。現在はどちらの更新スクリプトも、すでにインストールされているラッパーを再コピーし、存在しないものは作らない。併せて三つの誤ブロックも解消した。`git commit --verbose` はステージ済みの差分をはさみ線の下に**コメントなしで**置くため、その差分に禁止トレーラーを追加する行があるとメッセージが清潔でもコミットがブロックされていた。`set -e` と `git rev-parse` のコマンド置換の組み合わせは、リポジトリ外で終了コード 128、出力なしでフックを終わらせていた。そしてコメント文字は常に `#` とは限らず設定可能である `v1.26.104`
- **ファイルを移動することは、ファイルを書くことではなくなった** — アーカイブ済みの 22 個のフォルダを `git mv` で移動しただけ、一バイトも編集していないのに、pre-commit の機密スキャンが「認証情報が含まれている疑い」として commit をブロックした。名指しされた二つのファイルはコミット済みの版とバイト単位で同一であり、git 自身も `R100` と報告している。同じ内容は何バージョンにもわたってコミットされ続け、一度も検出されたことはなかった。スキャナーが誤読したのではない — スキャナーは git に**単一パス**の差分を求めており、パスを一つに限定すると、追加と対にすべき削除側が存在しないためリネーム検出が働かず、git はファイル全体を新規追加として返す。報告が提案した blob ハッシュの比較は、完全一致のケースを隠すだけで、より厄介なもう一方には効かない。**ファイルを移動しつつ一行だけ編集する**と、手つかずの残り 400 行はやはり新規として提示され、しかもハッシュは実際に変わっている。そこで両方のパスを git に渡すようにし、二つの呼び出しのいずれにも `-M` を明示した — 設定で `diff.renames=false` にしていると移動は無関係な削除と追加として届き、バグがそのまま戻るからであり、その設定の好みが commit をブロックするかどうかを決めてはならない。続くレビューが、この変更で唯一「以前よりスキャン量が減りうる」経路を突き止めた。git は返してきたパスを、こんどは **pathspec（パターン）**として読み直し、`--` ではそれを無効化できない。`:!victim.txt` は正当なファイル名であると同時に除外パターンでもあり、自分の移動先を自分の差分から打ち消してしまう — 実測で、スキャナーは新規追加された `sk-proj-…` の行を通過させた。`--literal-pathspecs` で修正しテストで固定した。同じフラグは逆方向の誤ブロックも併せて解消する。**新しい適用除外は追加していない**：blob SHA によるスキップは一度書いてから削除した。ペアリングを直した後では、その分岐があるかないかをテストで区別できず、ブロックを伴うセキュリティ検査においてテストされていない「スキャンを飛ばす」分岐は、安全の上積みではなく穴の追加だからである。影響は一度の commit にとどまらない — 誤検出を回避する手段として文書化されているのは bypass であり、bypass は同時に他のすべての規則も無効にする `v1.26.103`
- **コレクターが止まったことを、本人にも管理者にも通知するようになった** — 利用状況の収集はスケジュール実行のスキャナーに依存しており、そのスケジュールが死ぬと本人の MCP は毎日ハートビートを送り続ける一方で何もアップロードされず、ダッシュボード上は「この人は働いていない」とまったく同じに見える。原因となる機構はすでに二度修正済みだが（v1.26.65、v1.26.79）、2026-08-07 に計測した端末はなお十一日間沈黙しており、クライアントが古すぎてどちらの修正も届いていなかった。「N 日間報告がない」という規則ではこれをまったく検知できない。手がかりは**一台の端末が自分自身と食い違うこと**である。その端末の `claude-code` 行は 0.2 日前、他の三つは 11.2 日前だった。同じスナップショットで、健全な十台はすべてのツール行が同一秒に書かれていた。よってこの規則を採用し、二つのしきい値の間には意図的に隙間を設けた。判断がつかない端末には「かもしれない」ではなく何も出さないためである。本人には何が止まったか、いつからか、その間の利用状況が送られていないこと、そして修復方法を伝える。管理者には修復手順を含まない別の一覧を送る。他人の端末では実行できないからである。修復すれば通知は期限切れを待たずに早期終了する。初めて異常を検知した時点では通知しない。二週間ぶりに電源を入れた端末は、スキャナーが次に走るまでまさにこの形になる（Windows では二時間、バッテリー駆動ではさらに長い）。そのため六時間後もなお壊れている場合にのみ通知し、二週間経っても直っていない端末は再度知らせる。通知そのものは 48 時間で消えるからである。**完全に沈黙した端末は意図的に報告しない** — このデータでは、電源が切られた端末、休暇中の人、買い替えられたノート PC と区別がつかない `v1.26.102`
- **インデックス予算の半分が、セッションが既に持っているものの再掲に使われていた** — v1.26.100 は予算を件数で等分したが、等分は中立であり、ここでは中立が誤りだった。載せないことの代償が両者で違うからである。鉄則は SessionStart フックがセッションへ一式注入するので、インデックスの一行が足すのはローカルファイルへのリンクだけ。一方、載らなかったプロジェクトはセッションに存在しない。件数で等分した結果、予算の半分が再掲に費やされた（鉄則 63 件 対 プロジェクト 63 件、プロジェクト 67 件が未掲載）。現在、鉄則は 20 行の固定枠のみを取り、残りは他に経路を持たない種別へ回る。同じデータで**プロジェクトは 106 件を掲載、この一連の作業の前は 47 件だった**。上限を課された種別も、載せなかった実数をそのまま報告する `v1.26.101`
- **記憶インデックスの半分がセッションに届かなくなっており、それを誰も告げていなかった** — インデックスは記憶が一件増えるごとに一行増えるが、読み手は 200 行で打ち切り、140 行未満を求める。実際のインストールで計測すると 284 行、鉄則 143 件とプロジェクト 130 件だった。鉄則が先に並び 151 行目で終わるため、**セッションに届いていなかったのはすべてプロジェクト**である。130 件のうち届いていたのは 47 件で、残る 83 件はしばらく読み込まれていなかった。ファイルには何の印も残らず、どちらの側も警告しないため、完全なインデックスとして読まれ続けていた。上限を超えさせたのは鉄則を全件列挙していたことだが、SessionStart フックはすでに鉄則一式をセッションへ注入しているので、その行は何も足していなかった。現在インデックスには越えられない行数予算がある：各種別で更新が新しいものから採用し、取り分を使い切らない種別は残りを他へ譲り、各行は 200 文字で `…` を付けて切り詰め、改行を含む題名は一行に畳み（さもないと一件が三行になる）、そして**各節が「何件を載せなかったか」と「どこで探せるか」を明示する**。同じデータで 139 行、273 件中 126 件を掲載し、残る 147 件は明示的に告げられる。プロジェクトは 47 件から 63 件へ増え、鉄則は 143 件から 63 件へ減るが、失われるのはローカルファイルへのリンクだけで、規則自体はフックが注入する。記憶ファイルは一つも削除されず、到達不能になるものもない `v1.26.100`
- **「最も多いプロジェクト」列が空だったのは、誰もプロジェクト名を送っていなかったから** — チームページが読むフィールドは `ownmind_log_session` だけが書き込む。AI がそれを呼ばなかった場合、サーバーはアクティビティログからセッションを再構築するが、どのイベントもプロジェクト名を運んでいなかった。一週間の計測では、四名は全セッションがこの再構築で、五人目も 95 件中 76 件がそうだった。つまりこの列は四名について完全に空、五人目についても五分の四が空だった。当初これは「復旧不可能」と判断されたが、それは誤りだった。この値は v1.17.37 以来 `CLAUDE_PROJECT_DIR` から導出されており、単にセッションログ以外の何にも同行していなかっただけである。修正は復旧ではなく送信すること：導出を `resolveProjectName()` の一箇所にまとめ、MCP と二つの SessionStart フックが送るすべてのイベントがこれを運び、再構築側は最初の一件ではなく最頻値を採る。**ディレクトリ名のみで、パスは決して送らない** — 名前は作業の文脈だが、パスはその人がファイルをどこに置いているかを示す `v1.26.98`
- **シークレット検出が、コードの三分の一を鍵と誤判定していた** — 最後の砦となるヒューリスティックは「鍵の文字集合から成る 20 文字以上」で発火する。これでは鍵と識別子を区別できない。同じ日に 2 回コミットがブロックされた。ファイル名と変数名であり、いずれもユーザーには「API キーのように見える」と伝えられた。本リポジトリ自身の追跡対象ファイルに含まれる 20 文字以上のトークンすべてで計測したところ、**10486 件中 3438 件が誤検出**だった。例外はすでに 3 つ追加されており、いずれも誤ブロックのたびに足されたものである。4 つ目を足しても同じことの繰り返しになる。規則が測っていたのは長さであり、区別すべき性質は語の構造だからだ。現在は鍵らしい文字の最長連続部分（`- _ .` は連続を切るが base64 記号は切らない）を取り、そのうち語らしい区間が占める割合を測る。同一の標本で誤検出は 333 件に減り（うち 279 件は本来ランダムな npm の整合性ハッシュ）、コードと文書のみでは 9082 件中 51 件、0.6% となった。実在する 13 種類の鍵形式はすべて検出され、途中で本物の穴も塞がった。スラッシュ区切りの塊が 3 つ以上ある値はファイルパスとして無条件に通されていたが、base64 のシークレットはまさにその形を取りうる。古い例外のうち 2 つは冗長として削除し、保証は許可リストではなく計測された比率として表現するようにした `v1.26.98`
- **アップグレード失敗の記録が、理由ではなく推測だった** — アップグレードスクリプトの各エラー報告は、手書きの一文を `detail` として送っていた。そして `detail` こそサーバー、管理コンソール、ヘルス通知に届く内容である。`git pull --ff-only failed (network or non-ff merge)` は、リモートに到達できなかったのか、ブランチが分岐したのか、ファイルがロックされていたのかにかかわらず同じ文面になる。ある端末で pull が失敗し、バックアップを復元し、7 秒後に自己診断が全項目通過したが、なぜ失敗したのかは誰にも答えられなかった。ログ末尾を運ぶはずの `context` フィールドは空で届いた。どこで失われるかは未確定であり、**実証できない原因は原因として記録しない**。そこで失敗したコマンドのログ末尾を `detail` にも入れるようにした。1 行に畳み、制御文字を除去し、両プラットフォームで同一の上限を課す。呼び出し箇所の一覧はテスト時に導出する方式に変え、手作業で漏れていた箇所が即座に見つかった `v1.26.98`
- **更新ロックはロックしていなかった** — 三つのプログラムが `~/.ownmind/.update-lock` を共有し、`~/.ownmind` での日次 `git pull` が同時に走らないようにしている。しかし実際に取得していたのは MCP だけだった。シェルフックはファイルの不在を確認した十行あとで `touch` しており、その間に隙間があるうえ、`touch` は既存ファイルに対しても成功する。結果として同時に走った四つのフックがすべて「取得」した。Node フックはロックを確認したあと、何も作成していなかった。あるアカウントのアクティビティログを読んでいて判明した：毎朝四つのフックが同じ秒に起動し、三つが `update_failed`、一つが成功を記録していた。アップグレードは毎回成功しており、六日間で十八件の「失敗」はすべて競合に負けた側だった。現在は三者とも排他作成で取得し、停止した実行が残したロックの回収は専用ファイルで直列化したうえで再確認するため、順番待ちの間に他者が取得したロックを削除することはない。競合に負けた場合は失敗ではなく `update_skipped`（理由 `lock_held`）として記録される `v1.26.98`
- **送信確認のゲートは、はじめからゲートではなかった** — `ownmind_report_bug` の説明は「バックエンドが自動入力の送信を HTTP 400 で拒否する」ため AI が `confirm_string` を自分で埋めてはならない、と述べていた。実際には拒否しない：これは文字列フィールドであり、サーバーは期待どおりの値の文字列が届いたことしか分からない。報告者はその方法で二件のバグ報告を提出して実証した。サーバー発行のワンタイム語句でも塞がらない — その語句を取得する呼び出し元こそ AI だからだ。コンソールでの承認も同様で、`POST /me/login` は AI がすでに持っているのと同じ api_key を返す。両者が同一の主体として認証される限り、サーバー側のどんな検査も両者を区別できない。よってその主張は削除し、代わりに区別を記録する：`confirmation_declared` は `user_typed` / `ai_filled` / `unknown`。名前はそれが「申告」であることを示し、欠落や未知の値は強い方ではなく `unknown` になる `v1.26.97`
- **手書きのリストは、自分が取りこぼしたファイルを報告しない** — `.gitattributes` は git フックを 1 ファイル 1 行で LF に固定していたが、`hooks/ownmind-git-commit-msg` はそのリストが書かれた後に追加された。Windows のクローンでは、三つのフックのうちそれだけが CRLF で届く。リポジトリ全体を走査すると、シバンを持ちながら `eol=lf` 規則のない追跡ファイルが 32 件あった。現時点の影響はゼロ（Git for Windows は CRLF のシバンをどちらの形式でも実行する。`GIT_TRACE=1` で確認済み）だが、シェル提供元が変わった瞬間に実害となる。規則をグロブに変え、`git ls-files` からリストを生成して未カバーがあれば失敗するテストを追加し、`install.sh` はフックをコピーする際に CR を除去する — `.gitattributes` はチェックアウトのみを制御するため、すでに CRLF を持つマシンは git に書き換えられることがなく、正規化比較によって `status` にも `pull` にも見えないからだ `v1.26.96`
- **導入前**：新しい会話を開いてコード依頼するも、古い構文を返したり独自のコーディングスタイルを忘れたり。「うちのプロジェクトはこう書く」と毎回再入力。
- **導入後**：会話オープンの瞬間、OwnMind が既に好み・スタイル・剛性制約を AI に注入済み。要件だけ伝えれば、一発で準拠コードが出てきます。

### ペイン 2：マシンとモデルの切り替えで「知能ゼロ・規格分断」

- **導入前**：朝は会社デスクトップで Claude Code、午後は家のノート PC で Cursor + Gemini。**異なるマシン** + **異なる AI モデル**を跨ぐとメモリ文脈が完全リセット、コード再コピー＆モデル毎の癖を再調整。激痛。
- **導入後**：どの PC・どの AI モデルでも、OwnMind がバックグラウンドでメモリコアを統一。**どこで会話を開いても、連続性のある一貫体験。**

### ペイン 3：同じ失敗を繰り返す AI

- **導入前**：先週デプロイで env var を漏らして本番 DB がクラッシュ、徹夜修復。今日 AI に新機能デプロイを頼んだら同じ env var を漏らした指示を生成、再災害寸前。
- **導入後**：失敗時に「この教訓を覚えろ」と AI に伝えるだけ。鉄則として OwnMind にアップロード。次回 AI がデプロイしようとすると、カーネルレベルで阻止され、AI に自己点検を強制、再災害を未然防止。

### ペイン 4：チーム開発がバラバラ、統一基準なし

- **導入前**：チームが拡大するにつれ、各人の AI 使用習慣と規範が異なる。テストなしの粗悪コード、誤って機密 commit。標準ドキュメントを書いても誰も読まない。コード品質が制御不能に。
- **導入後**：リードが管理コンソールでチームルールを一度公開、全メンバーの AI が自動強制ロード・実施。非準拠コードは、ローカル pre-commit hook がハードリジェクト、開発者 + AI に修正を強制してから commit 許可。完全自動のチーム標準化と品質ゲート。

### ペイン 5：中央管理なし、チームの AI 使用状況が不明

- **導入前**：リードはチームの AI 使用状況を全く把握できない。誰がどのツールを使い、どの技術的摩擦が頻発し、ルール遵守率は実際どれくらいか — 群盲象を撫でる。データがなく真の生産性とリスクを評価できない。
- **導入後**：専属の**中央管理コンソール + データダッシュボード**。チーム全体の AI 使用状況、メンバー毎ルール毎の実コンプライアンス率、最頻出の落とし穴 — 主要メトリクスを一目で把握。コンプライアンス分析と監査ログ（audit log：改ざん防止された全アクション記録）が自動集計。

---

## 落とし穴進化論：ミスから鉄則への学習チェーン

OwnMind のコア価値は、日々の開発で「踏んだ落とし穴」を瞬時に AI の「遺伝子ルール」に沈殿させ、同じミスがチーム内で二度と起きないことを保証する点です：

```mermaid
graph LR
    A["開発で落とし穴 (例：テストなしで commit)"] --> B["AI に: この教訓を覚えろ"]
    B --> C["AI が自動抽出・鉄則化"]
    C --> D["中央サーバーが配信"]
    D --> E["全チームメンバー＆ツールが自動ロード"]
    E --> F["AI が次に違反試行 → ツールと Git commit がハードブロック"]
```

1. **捕獲**：デプロイや開発で問題発生時、AI に「この教訓を覚えろ：デプロイ前は主 DB 接続を必ず確認」と一言
2. **抽出**：AI が現在の文脈を統合し、機械検証可能な剛性鉄則（例：`IR-038`）に蒸留、OwnMind にアップロード
3. **継続的ゲート**：ルールが即座に全開発ツールに同期。次回どこで開発しても、AI が「接続未確認のままデプロイ」を試みた瞬間、システムがカーネルレベルで阻止

---

## チーム執行防衛線：開発規範をどう強制実施するか

プロジェクトが拡大すると、チーム管理は災害の始まり — 開発標準ドキュメントは誰も読まず、新人のコードはバグだらけ。OwnMind はチーム向けに強制実施システムを設計：

### 1. 三階層ロール権限管理

ビルトインの**スーパー管理者**>**管理者**>**一般ユーザー**の三層構造。上位二つはチーム鉄則の絶対制御権を持ち、一般ユーザーや AI が会話中に勝手にコア安全ルールを変更・無効化することを防止。

### 2. チーム標準の一括配信

リードは管理コンソールで一度ルール公開（例：「全返答にリクエスト ID を含む」）すれば OK。システムは RAG（Retrieval-Augmented Generation：外部ナレッジを検索してから AI に回答させる技術）で意味的にスライシング。メンバーが新しい会話を開いた瞬間、AI が要約を強制ロードして厳密実施し、詳細は必要に応じて取得。人手による告知や標準同期コストゼロ。

### 3. ローカル Git pre-commit ハードゲート

チーム管理の最強物理防衛線。OwnMind は全メンバーの PC に pre-commit hook をインストール。メンバーの AI がチーム鉄則違反コード（例：機密パスワード、文書同期漏れ）を書くと、`git commit` はハードリジェクト、開発者 + AI に修正後の再 commit を強制。

### 4. コンプライアンスダッシュボード + 監査ログ

管理コンソールが各メンバー・各 AI モデル・各鉄則の実行データ（遵守回数、トリガー回数、違反回数）を自動追跡しコンプライアンス率トレンドグラフ化。全 AI アクション、違反、バイパス例外申請が改ざん防止監査ログに記録、データ駆動型品質管理の確固たる基盤に。

---

## 剛性安全ゲート：三道の防衛線

プロンプトで AI にお願いするだけでは効きません。OwnMind はローカル環境と OS レベルで、AI が痕跡を残さず突破できない三道の物理防衛線を構築：

1. **第一線：仕様統一** — 会話オープン時、システムが API 経由で底層プロンプトを注入。意味的防衛線。
2. **第二線：実行側コントロール** — AI が実際にコード変更やコマンド実行する前に **PreToolUse 介入**（PreToolUse：AI がツール呼出する前の安全ゲート機構）。コードを読まずに変更しようとすれば即拒否＆ロールバック、**ブラインド編集**（Blind Edit：コード未読のまま直接ファイル変更）を阻止。
3. **第三線：出力側レビュー** — AI が出力完了時、Stop hook（Stop hook：AI が応答完了時に自動起動されるレビュープログラム）が reply-lint を起動。中英混在・専門用語の説明不足・身分証/メール/携帯番号の偶発漏洩（v1.19.7 で privacy_check 検出器追加）を検出したら、システムが背景で AI に**書き直し**を命じ、準拠するまで通しません。**連続 3 回ブロック後は自動的に警告にダウングレード**、AI の無限書き直しループによる対話デッドロックを防止。

---

## プロジェクト直下の静的ルールファイルとの違い

「プロジェクト直下の静的ルールファイル（例：`.cursorrules`、`CLAUDE.md`）で十分では？なぜ OwnMind？」

静的ルールファイルは脆弱（AI ハルシネーションで密かに迂回される）です。OwnMind はステートフルな剛性安全ゲートシステム：

| 比較項目 | プロジェクト直下の静的ルール | OwnMind ステートフル強制実施 |
|:---|:---|:---|
| **メンテナンス** | プロジェクト毎にファイル散在、同期困難 | 中央サーバー一箇所修正で全マシン・全ツール即同期 |
| **強制力** | AI の自律頼み、ハルシネーションで迂回 | critical 級ルールはプログラムによる強制中断。ローカル pre-commit hook で git commit 自体をブロック |
| **エラー学習** | 毎会話がまっさら、同じ落とし穴を繰り返す | 違反を自動記録、適応的強化機構（システムが AI の違反履歴に基づきプロンプトを動的強化）を発動 |
| **情報セキュリティ** | 機密キーやパスワードを誤って commit しやすい | 機密文字列を自動フィルタ・ハードブロック、安全な金庫へ誘導 |
| **チーム展開** | メンバーが手動コピー、漏れたら未管理 | リードが一括配信、新人初日から自動整列 |

> **正直な開示**：`OWNMIND_BYPASS` 環境変数（ルールバイパススイッチ）は緊急時に単一鉄則をスキップ可能 — ただし全バイパスは監査ログに記録。AI は痕跡を残さずに迂回できません。

---

## コア機能

### メモリと保護

- **メモリ / シークレットルーティング** — 三種のデータ分離：profile / memory（通常メモリ）/ secret（暗号化金庫）。AI が通常メモリにパスワード書込み → HTTP 400 でブロックし金庫へ誘導 `v1.19.1`
- **個人情報検出器（オプション）** — AI 返答ごとに台湾身分証（公式チェックサム検証付き）、メール、台湾携帯番号をスキャン可能。user 自身がプロンプトで提示した文字列は能動的共有とみなし例外扱い。検出時 `privacy_check` イベントを発火、ブロック可否は各 user の鉄則で決定 `v1.19.7`
- **Pre-commit シークレット内容スキャン** — 既存のファイル名ブロックに加え、staged diff の追加行を OpenAI / GitHub PAT（Personal Access Token）/ JWT（JSON Web Token）/ AWS key パターンで IR-002 違反として検出 `v1.19.7`

### コラボレーションと同期

- **クロスツール引き継ぎ** — 終業時に AI へ「wrap up して引き継ぎ準備」と一言で、未完タスク＆設計文脈を自動パッケージング＆アップロード。明日別エディタ＋別 AI でシームレス継続 `v1.13.0`
- **マルチクライアント衝突ロック** — 複数エディタが同時にメモリ書込み時、同期トークン（誰が先か判定するタグ）が古いバージョンをブロックし AI に最新再取得を強制。苦労して蓄積した開発ルールが上書きされません
- **クロスマシン自動同期** — ノート PC 切替、出張、新人初日 — ログインで即メモリ＆ルール取得、待ち時間ゼロ

### 観測性と分析

- **三段階ルール分類** — 全ルールに `critical`（赤、強制ブロック）/ `default`（黄、設定次第）/ `advisory`（灰、ログのみ）のラベル。SessionStart ダイジェストでティア別にグループ表示 `v1.19.0`
- **コンプライアンスダッシュボード** — メンバー毎・AI モデル毎の遵守 / トリガー / 違反回数を追跡、トレンドグラフ化
- **返答品質リント** — Stop hook が中英混在（IR-037）、専門用語の説明不足（IR-036）、個人情報パターン（`privacy_check` イベント、各 user の鉄則がブロック可否を決定）をスキャン。session 内 4 回目の違反で `process.exit(2)` + stderr に指令型書き直しプロンプト出力。連続 3 回ハードブロック後は警告に自動降格しデッドロック回避 `v1.19.7`
- **バージョン強制即時チェック** — 新規会話毎に API でクライアント / サーバーバージョン確認。修正済みバグに古いバージョンで踏むのを防止 `v1.19.4`
- **インストールチェック失敗レンダリング** — 純粋関数がまとめたインストールチェック失敗を読みやすいメッセージに変換。同じ問題を抱える複数マシンは 1 行に集約。読み手に実際に表示される範囲（配信側は先頭 5 行・400 文字のみ）に合わせて生成するため、1 件の失敗は 1 行、省略件数は最終行、切り詰められた項目には必ず可視マーカーが付く `v1.26.87`
- **インストールチェック警報ジョブ** — `runInstallCheckAlerts` が各マシンの最新セルフチェック結果（クライアント送信のタイムスタンプではなくサーバー採番の id 順）を読み、新規／解決済みの失敗を記録し、最も在籍歴の長い super admin 宛てに有効期限 48 時間のブロードキャスト草稿を作成。新規失敗は先に「確保」してからブロードキャストを書き込み、確保もブロードキャストもすべて 1 つのトランザクション内で実行する。途中のどこかが失敗すれば全体がロールバックされ、例外が再送出される。そのため同時実行された 2 つの走査でも通知は 1 回だけ、書き込み途中の中断が「通知済みなのにブロードキャストが存在しない」失敗を残すこともない（確保がサーバー側で確定した後に応答だけが失われた場合も含む）。レポート保存ごとに 1 回実行（`POST /api/debug/install-check`、失敗はログのみで例外は投げない）、サーバー起動時にも既存レポートを対象に 1 回補完実行 `v1.26.87`

### 鉄則執行エンジン

- **最も多くタグ付けされた鉄則が、一度も発火していなかった** — フックは `Bash` にしか登録されておらず、規則はシェルコマンドの実行中にしか発火できなかった。ファイルの編集はシェルコマンドではないので、`trigger:edit` が付いた規則は AI がコードを変更している最中に一度も提示されたことがなかった。実際のアカウントでは 56 件（そのアカウントで最も使われているタグ）、別名と全トリガーに一致する未タグ付けの規則を数えると 68 件に対し、提示は 0 件。今後はファイル編集ツールでも実行され、1 時間単位で抑制される：その時間の最初の 1 回は全件を列挙し、それ以降の編集は 1 行だけ — `AI 改檔案要遵守的鐵律 68 條 · 本小時第 4 次`。主語を AI にしているのは、「68 件の鉄則が有効」では画面を見ている人への指示に読めるからであり、規則が適用されることは述べても、守られたことは述べない — フックは前者を観測できるが後者はできない。リマインダーのみ：edit トリガーは検証エンジンに到達せず（ここで唯一ブロックしうる経路）、抑制された 1 行はネットワークにアクセスしない `v1.26.92`
- **シェルフックが記録したフィールドは、到着時にすべて捨てられていた** — `log_event` はキー/値のペアを `ts` や `event` と並ぶトップレベルに書き、その同じオブジェクトをバッチエンドポイントへ送っていた。しかし受け側は `e.details` だけを読む。`details` は一度も存在せず、すべての行が `{}` として保存されていた。アップグレードフックは lock / cd / fetch / pull / npm / update_sh の六つの失敗ステップを区別するが、そのどれもユーザー自身のマシンから出たことがなかった：2026-08-07 の計測で、あるユーザーは 18 回、別のユーザーは 9 回アップグレードに失敗しており、理由の記録は一件もなかった。同じ沈黙が「どのトリガーが発火したか」と、一つ前のリリースで追加された `edit_reminder_failed` も覆っていた — つまりその通知経路は生まれた時点で壊れていた。ペアは `details` の中に入るようになった。サーバー側の変更は不要で、最初から正しいキーを読んでいた `v1.26.95`
- **Windows の MCP パスが settings.json に書き込まれる途中で壊れなくなった** — `cygpath -w` はバックスラッシュ区切りのパスを返すが、install.sh はそれを `node -e` のソーステキストへ直接埋め込んでいた。パーサが受け取っていたのは `const p = 'C:\Users\Vin\.ownmind\mcp\start.cmd'.replace(/\\/g, '\\\\')` であり、`\U`・`\V`・`\.`・`\m`・`\s` はいずれもエスケープシーケンスではない。JavaScript はバックスラッシュを捨てて文字だけを残すため、バックスラッシュを二重化するために置かれていた `.replace` が走る前から `p` は既に `C:UsersVin.ownmindmcpstart.cmd` になっていた。`bootstrap.sh` でアップグレードするたび、この実行不可能なコマンドが `~/.claude/settings.json` に書き込まれていた。気づかれなかったのは、Claude Code が MCP サーバーを `~/.claude.json` から起動し、そのファイルをどのインストーラも書かないためで、壊れた値は誰も読まないファイルに残り、次の install.ps1 実行が静かに直していた。これはエスケープでは解決できない — 文字列を壊しているのはシェルではなく JS パーサだからだ。現在はどちらの分岐もパスを argv 要素として Node に渡す `v1.26.94`
- **新しいキーでインストーラを再実行すると、実際にキーが変わるようになった** — どちらのインストーラも、設定ファイルに `"ownmind"` という文字列が含まれていれば Claude Code の MCP ブロックをまるごとスキップしていた。しかしそのブロックこそ API キーの置き場所であり、キーを変えるための再実行 — アカウント切り替え、認証情報のローテーション、打ち間違いの修正 — はすべて何もせず、そのままインストール完了サマリーを表示していた。判定すべきは「このキーで設定されているか」だったが、実際には「設定されたことがあるか」を問うていた。Cursor の MCP ブロックも同じガードと同じキーを持っていたので一緒に修正。どちらも常に書き込み、上書きではなくマージするので既存エントリのうちインストーラが管理しない項目は保持される。実行後は「書き込み／更新／変更なし」のどれだったかを明示する。以降の「設定済みのためスキップ」はルール文言の追記で認証情報を含まないため、そのまま `v1.26.93`
- **2 つの設定ファイルが別々のキーを持つ状態が見えるようになった** — インストーラは `~/.claude/settings.json` しか書かないが、Claude Code は MCP 設定を `~/.claude.json` に持つ。`resolveCredentials` は最初に見つけたキーを返すため、アカウント切り替え後に両ファイルが食い違ったままでもすべてのチェックが通り、もう一方のファイルから起動されたプロセスは別アカウントとして動作しうる — 値を比較するコードはどこにもなかった。`resolveCredentials` は `conflicts` を返すようになり、新しい `credential_agreement` セルフチェックが警告して食い違うファイル名を示す。比較対象はファイルのみ：環境変数は検索順の最後で負ける側になりえず、数に入れるとインストール時のキーをシェルに保持している全員に警告が出てしまう。値ではなく場所のみ — これはレポートと共にアップロードされる `v1.26.93`
- **鉄則は「意味」で一致させる。三つの単語との完全一致ではない** — `detectCommandTrigger` が返すのは `commit` / `deploy` / `delete` の三語だけで、フックはタグがちょうど `trigger:<その語>` の規則しか残さなかった。しかしその三語が語彙のすべてだとはどこにも書かれておらず、`ownmind_save` はどんなタグでも受け取る。だから規則は作者が考えるままの言葉 — `trigger:回滾`、`trigger:cleanup`、`trigger:升級` — で保存され、フィルタで捨てられ、理由を告げないまま静かに終了する。実際のアカウントで、三つの鉄則がどの操作からも到達できない状態が見つかった。現在は各トリガーが同義タグの集合を受け付ける（`delete` は 刪除／cleanup／清理／rollback／回滾／還原／restore なども受け付ける、以下同様）。比較は大文字小文字を区別しない。これは「どの規則が一致するか」を広げるだけで、「フックがいつ走るか」は変えていない — 発火条件は従来どおり `detectCommandTrigger` のままなので、うるさくなることも、新たに止められることもない。`trigger:install` のような高リスク操作の範囲外のタグは依然として発火しない。`npm install` はそもそもフックが走る操作ではないからだ `v1.26.91`
- **実行前の鉄則リマインダーがようやく発火する** — フックはトップレベルの `.command` を読んでいたが、Claude Code が送るのは `{ tool_name, tool_input: { command } }`。抽出結果は常に空で、フックは空値ガードで正常終了していた — プラットフォームを問わず誰もリマインダーを見ていなかった。Windows ではさらに手前で失敗していた：`readFileSync('/dev/stdin')` は `C:\dev\stdin` に解決され `try` の外で例外を投げ、`2>/dev/null` がそれを捨てていた。両方のフックを fd 0 読み取りと `tool_input.command` 優先に修正し、旧形式は手動実行用に残した `v1.26.90`
- **一致した検証テンプレートは提案であって適用ではない** — 鉄則の保存時にサーバーが `metadata.verification` を黙って付与していた。セット内の全テンプレートが `block_on_fail: true` を持つため、リマインダーとして書いたルールが作業を止めるルールとして返り、通知はレスポンス内に埋もれた ID ひとつだけだった。長い本文中のキーワード 1 個で一致する: ロールバック時にログを消さないという趣旨のルールが、デプロイのタグと本文中に一度だけ現れる「測試」により「デプロイ前にテストを実行」を付与され、ブロック時の文言は作者が書いていない内容を表示していた。一致判定自体は残し、`template_suggestion`（名称・`applied: false`・作業を止めるか・そのまま伝えられる一文）として返す。誰かが求めるまで何も書き込まない `v1.26.89`

- **共通ジャッジコア** — 純粋関数 `enforceRule(ruleCode, context, options)` が `allow` / `block` / `warn` / `log_only` / `bypass` をティアに基づき返す。**ステータス：**v1.19.7 では `bypass-handler.js` のみを git pre-commit と reply-lint に接続（`OWNMIND_BYPASS` 環境変数が機能する）；`evaluateConditions` ループを `enforceRule` 呼び出しに置き換える完全統合は v1.19.8 に延期 `v1.19.6`
- **適応的強化** — システムが AI の違反履歴に基づきプロンプトを動的強化（同一ルールを 1 session 内で 3 回以上違反 → 次回そのルールのプロンプトが自動で強警告にアップグレード）

### インフラストラクチャ

- **シークレット管理** — API キー・パスワードを二重暗号化（master key + per-row salt）でセキュアに保管
- **キーワード検索** — タイトル・内容・タグ・コード欄を対象とした複数キーワード AND 検索（データベースには pgvector 列を予約済みですが、意味的類似度検索は将来のアップグレード項目で現時点では未稼働）
- **段階圧縮** — 短期メモリ自動圧縮、長期メモリ永久保存
- **Windows ネイティブサポート** — `install.ps1` と `start.cmd` 同梱、WSL（Windows Subsystem for Linux）不要
- **ソースファイル検索可能性ガード** — `src/`、`scripts/install-helpers/`、`hooks/` 配下のファイルに生の制御バイト（NUL など）や実体の不可視文字が含まれていると `grep`／`file` がバイナリと誤判定して黙って読み飛ばす。テストスイートがこれを検知して失敗させる `v1.26.87`
- **インライン Node に渡すパスを Windows 形式へ統一** — Git Bash 上では POSIX パス（`/c/Users/...`）を `node -e` のソース文字列に埋め込むと node.exe が変換せず受け取り、先頭のスラッシュをドライブルート基準で解決するため、インストーラーが途中で何も出力せず停止していた。現在はすべて共用の `to_win_path`（`cygpath -m`、Windows 以外では恒等）を経由し、ガードテストが対象一覧をスクリプト自身から生成する。解析できないブロックはスキップではなく失敗として扱う `v1.26.88`
- **インストーラーは Node のエラー出力を捨てない** — `set -e` のスクリプトで `2>/dev/null` を使うと、致命的なエラーが「ただ停止するスクリプト」になる。Node の stderr はログへ送り、ERR trap が中断行を示し、アップグレードログはロールバックが置き換えるディレクトリの外に置く。「ログを見てください」がロールバック直後に削除済みのファイルを指すことはなくなった `v1.26.88`
- **インストール完了はバージョン番号ではなく成果物で判定** — バージョン番号はインストール完了の証拠にならない。インストーラーが途中で中断しても、別の仕組みが作業ツリーを進めていれば最新バージョンを報告してしまう。共用の成果物一覧（SessionStart フック、鉄則フック、`hooks/lib`、git hooks、スキルファイル、MCP エントリポイント）を各インストールの最後に検証し、同じ一覧をセルフチェックの `install_complete` 項目として報告するため、途中で切れたインストールは自ら申告する `v1.26.88`
- **フックメッセージのローカライズ** — 実行ゲートのブロック／確認メッセージや、他のフックが端末に出す通知が、あなたの言語で表示されるようになりました。明示的に設定した言語が最優先、未設定なら OS の言語、どちらもなければ英語。中国語・英語・日本語に対応、変更方法は下記 FAQ を参照

---

## システム要件

- **クライアント**：Node.js 20+、Git 2.30+
- **サーバー**：セルフホスト（Docker Compose ワンクリックデプロイ提供）、または認可ホスト型サービス利用
- **対応プラットフォーム**：macOS / Linux / Windows（Windows ユーザーは Git for Windows フルインストーラ必要、Git hook が sh.exe を使用）
- **対応 AI ツール**：Claude Code、Codex CLI、Cursor、Copilot、OpenCode、Windsurf、Gemini CLI など MCP（Model Context Protocol：AI と外部ツールの統合標準）またはフックを読めるクライアント

---

## クイックスタート

### 1. API キーと URL を取得

OwnMind インストール後、管理者として管理コンソールにログインしアクセスキー（API key）とサーバーパス（API URL）を取得。

### 2. インストール方法を選択（二択）

#### 方法 A：AI 自動インストール（最も簡単）

AI エディタ（例：Cursor または Claude Code）のチャットに入力：

- **新規インストール**：`OwnMind をインストールして（key は YOUR_API_KEY、URL は YOUR_API_URL）`
- **自動アップグレード**：`OwnMind をアップグレード`

AI が OS を自動検出してインストーラを実行。

#### 方法 B：ターミナルのワンラインコマンド

macOS または Linux：

```bash
# 新規インストール
curl -fsSL https://raw.githubusercontent.com/miou1107/ownmind/main/scripts/bootstrap.sh | bash -s -- YOUR_API_KEY YOUR_API_URL

# アップグレード
curl -fsSL https://raw.githubusercontent.com/miou1107/ownmind/main/scripts/bootstrap.sh | bash
```

Windows PowerShell：

```powershell
# 新規インストール
$env:OWNMIND_API_KEY='YOUR_API_KEY'; $env:OWNMIND_API_URL='YOUR_API_URL'; iwr -useb https://raw.githubusercontent.com/miou1107/ownmind/main/scripts/bootstrap.ps1 | iex

# アップグレード
iwr -useb https://raw.githubusercontent.com/miou1107/ownmind/main/scripts/bootstrap.ps1 | iex
```

> **Windows 注意**：OwnMind は Git for Windows 同梱の Bash シェル（sh.exe）に依存して Git hook を実行します。フル版の Git for Windows をインストール（Lite や Portable は不可）、さもないと commit 時にエラー発生。

### 3. インストール検証

インストール後、新しい会話を開いて AI に：

> 「現在ロードされている OwnMind 鉄則を最初の 5 件見せて」

AI がリストできれば（IR-002、IR-008 などのコード付き）、hook が正しくメモリ注入できています。

リストできない場合は確認：

```bash
cat ~/.ownmind/credentials   # key 存在確認
cat ~/.ownmind/cache/iron_rules.json | head -3   # ルールキャッシュ確認
```

---

## 緊急バイパス：詰まった時の対処

OwnMind は env-var 経由の脱出口を提供。**全てのバイパスは監査ログに記録**、後でリード（または将来の自分）が確認可能。

### pre-commit がブロック、とにかく commit したい

```bash
# 単一ルールをバイパス
OWNMIND_BYPASS=IR-002 git commit -m "hotfix: 緊急"

# 全ルールをバイパス（極端な場合）
OWNMIND_BYPASS=all git commit -m "hotfix: 緊急"
```

### reply-lint が煩い、警告のみに退避

```bash
# 違反は警告のみ、AI 返答ブロックなし
export OWNMIND_REPLY_LINT_MODE=warn

# または完全スキップ
export OWNMIND_REPLY_LINT_DISABLE=1
```

### OwnMind 全体を一時停止

```bash
export OWNMIND_DISABLED=1
```

> **重要**：これらは脱出口であって長期設定ではありません。特定ルールを常時バイパスしているなら、そのルール自体が設計ミス — 管理コンソールで修正する方が OwnMind を停止するより良いです。

---

## よく使うチャットコマンド

AI に普通の言葉で命令：

- **「この教訓を覚えろ：[内容]」** — 自動で鉄則を生成、全ツールに同期
- **「このプロジェクトで残ってる作業は？」** — AI が OwnMind から横断的 TODO リストを引き出す
- **「今日の作業をまとめて引き継ぎ準備」** — 進捗を自動パッケージング、明日別マシンや別ツールでフル回復
- **「私のバージョンは最新ですか？」** — オンラインの最新バージョンを強制チェック
- **「鉄則を最初の 10 件見せて」** — ローカルルールキャッシュの状態確認

---

## よくある質問 FAQ

### インストール関連

#### Q: 新規サーバーデプロイ後、初回の管理コンソールへの入り方は？

**v1.19.8+ 推奨フロー（摩擦ゼロ）**：
1. `docker compose up -d` でサーバー起動
2. ブラウザで `https://あなたのサーバー/admin` を開く — システムが `users` テーブルが空であることを検出し `/setup` に自動 redirect
3. email + パスワードを入力して最初の super_admin を作成
4. ページに api_key（ワンクリックコピー）+ client インストールテンプレート（現在のサーバー URL 自動入力済）が表示
5. 「ログインへ」をクリックして管理コンソールへ入り、他のメンバー作成や鉄則管理を開始

所要時間：約 2 分。Setup wizard は最初の admin 作成後、永続的に自動クローズ。

**進階／レスキューパス（v1.19.7 以前の旧フロー）**：

下記シナリオは wizard で対応不可：

| シナリオ | 経路 |
|:---|:---|
| バックアップから復元、`users` テーブルに super_admin はいるが `password_hash IS NULL` | 環境変数 `SETUP_TOKEN` を設定し、管理画面のログインページへ。パスワード設定フォームが表示されます（`POST /api/admin/setup`） |
| 管理者がパスワードを忘れて再設定したい | DB に SSH、`UPDATE users SET password_hash = NULL WHERE id = ...`、その後 setup token パス利用 |
| サーバー移行、旧 DB をインポート | pg_dump / pg_restore + 上記いずれかのレスキューパス |

通常の初回インストールは `SETUP_TOKEN` **不要** — wizard が完全カバー。

#### Q: Admin がパスワードを忘れたら？（v1.19.9+ 三層レスキュー）

「他の admin がいるか」で経路が決まる：

**シナリオ A：チームに他の admin がいる（最も一般的）**
- 他の super_admin が管理コンソールにログイン、「使用者管理」でロックされた admin を見つけ「パスワードリセット」をクリック
- システムが 12 文字のランダム一時パスワードを生成（混同しやすい 0/O/I/l/1 を除外）、本人にコピーして渡す
- 本人は一時パスワードでログイン、強制的に新パスワードを設定

**シナリオ B：唯一の admin がパスワードを忘れた**
- サーバーホストに SSH してレスキュースクリプトを実行：
  ```bash
  node scripts/reset-admin-password.js
  ```
- スクリプトはインタラクティブ：全 super_admin を列挙、リセット対象を選択、`yes` 入力で確認
- ランダム `SETUP_TOKEN` を生成・ターミナルに表示
- 続いて `export SETUP_TOKEN=<token>`、サーバーを再起動し、管理画面のログインページで該当アカウントのメールと任意のパスワードを入力します。パスワード未設定と判定され、設定フォームが表示されます。（v1.26.59 より前は `/admin/setup` と記載していましたが、旧管理画面は同バージョンで廃止されました。）

**シナリオ C：SSH 権限なし（クラウド SaaS モード）**
- 現状ビルトイン経路なし — サービス提供者に連絡
- v1.20+ で email リセットフロー追加予定（SMTP 統合に依存）

**予防は治療に勝る**：v1.19.9+ から、admin が 1 人だけの時は管理コンソールにオレンジ警告バナーを表示、第二の admin 作成を強く推奨。

#### Q: `.cursorrules` や `CLAUDE.md` と共存可能？

可能。OwnMind は中央集中、`.cursorrules` はプロジェクトローカル補完。推奨：プロジェクト横断の個人好み・剛性鉄則は OwnMind、単一プロジェクト特殊ルールは `.cursorrules`。衝突なし。

#### Q: なぜ AI が時々同じ内容を 2 回言うように見える？

バグではなく、返答品質チェック機構の可視的な副作用です。フロー：

1. AI の最初の返答が出力済み、リント hook が違反を検出（中英混在、専門用語の説明不足、個人情報の混入）
2. session 内 4 回目の違反で hook がブロック、AI に「書き直して」と指示
3. AI が準拠版を再出力 — ユーザーは似た 2 段落が見える

v1.19.11 で 3 層の改善を追加：

- **AI 自己アノテーション**（85% の信頼性）：書き直しの冒頭に引用ブロック「⚠️ 前版が IR-XXX に違反、再調整」+ セパレータ
- **段階表示**：第 1 回ブロックは完全メッセージ、第 2-3 回は短い「↻ 前版が IR-XXX に違反、書き直し済み」
- **永続ログ**：全ブロックイベントを `~/.ownmind/logs/reply-lint-events.jsonl` に追記、いつでも「今週何回ブロックされた、どのルールが最頻」を照会可

AI がアノテーションをスキップした場合（15% のケース）、ログでわかります：

```bash
tail -5 ~/.ownmind/logs/reply-lint-events.jsonl
```

### プライバシーとセキュリティ

#### Q: 会話データはどこへ？OwnMind 自体は個人情報を収集する？

OwnMind の設計原則は「業務直接関連でない限り user の個人情報を収集しない」。明細：

**サーバーへアップロード**：
- あなたが能動的に作成した鉄則、メモリ項目、profile の好み
- コンプライアンスイベント（どのルールがいつトリガーされ、結果は comply / violate / bypass か）— コンプライアンス分析用

**絶対にアップロードしない**：
- AI 完全会話内容（あなたの prompt + AI 返答原文）
- ソースコード / ファイル内容
- システムパス、ファイル名（あなたが能動的にメモリ化したもの以外）
- キーストローク、マウス軌跡などの telemetry（行動テレメトリ：ユーザー操作を監視するデータ）

**セルフホスト = 100% あなた自身のサーバー上**、「アップロード」されるデータもあなたのデータベース内のみ。

#### Q: API キーやパスワードはどう保管？通信は安全？

- **保管層**：API キーと `/api/secret` の機密は二重暗号化（master key + per-row salt：マスター鍵 + 行毎の固有ソルト）、DB 全体が盗まれても直接復号不可
- **通信層**：HTTPS 強制（HTTP 自動 redirect、nginx/caddy をフロントに置いて SSL 終端可）
- **ローカル層**：クライアント `~/.ownmind/credentials` は mode 0600（現在の user のみ読取可）
- **監査**：全 API アクションを audit log に記録、タイムスタンプ + 操作者 user ID 付、改ざん耐性

#### Q: OwnMind は私のソースコードを見る？

見ません。OwnMind の hook は**ルール違反イベント**のみを捕捉（例：staged ファイルに認証情報パターン、AI 返答の中英混在が閾値超）し、違反メタデータ（イベント自体を記述するデータ、コード内容は含まず）をサーバーに返送。**ソースコードと AI 会話内容は本機を離れません。**

例外：あなたが明示的に AI に「このコードスニペットをメモリに保存」と命じた場合、そのスニペットはアップロードされます。

### コストと性能

#### Q: OwnMind 使用で AI token 課金は増える？

増えますが少額です。OwnMind 自体は **LLM 呼出をしません**（GPT/Claude API コールなし）、追加 LLM API コストは発生しません。ただし新会話毎に鉄則リスト（30-50 件、約 2-5 KB のテキスト）を AI の system prompt に注入するため、新会話毎の AI input token が 1500-3000 token 増加（鉄則数次第）。

Claude Sonnet 料金（input $3 / 1M tokens）で計算：新会話毎に約 $0.005-0.01 の追加。1 日 20 新会話なら追加コスト約 $0.10-0.20/day。「好み再説明」で節約される時間を考慮すれば、ROI（投資対効果：投入と回収の比）は明確にプラス。

> 最適化のヒント：管理コンソールで現業務に影響しない鉄則を `disabled` に、または `advisory` ティア（広告級：ログ記録のみ、自動注入なし）に設定すれば token オーバーヘッド削減可能。

#### Q: hook で開発フローが遅くなる？

実測レイテンシ（M1 MacBook、ローカルキャッシュ温まり済）：

| Hook | レイテンシ | トリガー |
|---|---|---|
| pre-commit | < 50 ms | `git commit` ごと |
| reply-lint | < 30 ms | AI 返答ターン終了ごと |
| session-start | 200-500 ms | 新 AI 会話オープン（ルール読込込み） |
| version-check | < 100 ms | 新会話オープン（並行 API コール、非ブロック） |

知覚しうるのは session-start の 200-500 ms のみ、他は知覚閾値以下（< 100 ms は人間が感じない）。ネットワーク同期はバックグラウンドの非同期実行、commit を阻害しません。

#### Q: サーバーがダウンしたら作業が固まる？

固まりません。設計原則 fail-open（失敗時はブロックではなく許可）— サーバー無応答時 hook はチェックをスキップ、commit も AI 返答もブロックしません。ローカルルールキャッシュで 24 時間オフライン動作可能。24 時間経過後 hook が再同期試行、失敗時も fail-open。

### その他

#### Q: オフラインで使える？

可能。全鉄則は `~/.ownmind/cache/iron_rules.json` にキャッシュ、24 時間以内のオフライン動作は通常通り。

#### Q: 鉄則のインポート / エクスポート、チーム間共有は可能？

可能。各ルールには固有 UUID、管理コンソールが JSON エクスポートをサポート。異なる OwnMind インスタンス間で移行、または GitHub にオープンソース化して他チームに提供。

#### Q: なぜ「OwnMind」？

「Own your mind」— あなたのメモリはあなたのもの、AI ベンダーのものではない。新ツール毎にスタイル再教育するのは実質的にあなたの思考習慣（mental model：物事の考え方の癖）をベンダーに貸している。OwnMind が所有権をあなたに戻します。

#### Q: OwnMind 自身が表示するメッセージの言語は？

実行ゲートのブロック／確認メッセージや、他のフックが端末に出す通知が、あなたの言語で表示されるようになりました。これは管理コンソールの言語設定とは別物で、OwnMind はこの用途ではその設定を参照しません。

**判定順序（平易に）：**
1. 明示的に設定した言語が最優先。設定はそのマシンに同期した後、どのマシンでも同じ言語になります。
2. 未設定なら、あなたの OS の言語に従います。
3. どちらも対応言語に当てはまらなければ、英語で表示します。

**対応言語**：中国語（`zh`）、英語（`en`）、日本語（`ja`）。それ以外の OS 言語は仕様として英語にフォールバックします — エラーにはなりません。

**変更方法**：AI に「OwnMind の言語を日本語にして」のように伝えると、`ownmind_set_locale` ツールが `zh`／`en`／`ja` を指定して呼ばれます。OS の言語に戻したい場合は `auto` と伝えてください。

**反映タイミング**：設定したそのマシンでは即座に反映されます。他のマシンでは、次に新しい AI セッションを開始したときに反映されます。

> 日本語の文言はダッシュボード自身の `ja` 文言と同じ翻訳パイプラインで機械生成されたもので、まだネイティブスピーカーによるレビューを受けていません。

---

## API リファレンス

### 認証

全 API リクエストには `Authorization: Bearer YOUR_API_KEY` ヘッダが必要。

### 主要エンドポイント

```
GET    /api/memory               # メモリ一覧
POST   /api/memory               # メモリ作成
PUT    /api/memory/:id           # メモリ更新
DELETE /api/memory/:id           # メモリ削除
GET    /api/memory/type/:type    # タイプ別取得（例：iron_rule）

POST   /api/secret               # 暗号化シークレット作成
GET    /api/secret/:key          # 復号化シークレット取得
DELETE /api/secret/:key          # シークレット削除

POST   /api/activity/batch       # コンプライアンスイベント一括報告
GET    /api/compliance/stats     # コンプライアンス統計取得
```

完全な API ドキュメントは [OpenAPI spec](../openapi.yaml)（あれば）参照。

### メモリタイプ

- `profile` — 個人の好み（ニックネーム、慣用ツール、文体）
- `iron_rule` — 鉄則（検証条件、ティア、block_on_fail フラグ付）
- `project` — プロジェクト文脈（アーキテクチャ、技術スタック、TODO）
- `principle` — 作業原則（機械検証不可、AI 参考用）
- `learning` — 落とし穴記録（鉄則化前のドラフト）

---

## 技術スタック

- バックエンド：Node.js 20+ / Express
- データベース：PostgreSQL 16 + pgvector
- デプロイ：Docker Compose
- クライアント hook：Node.js（macOS / Linux / Windows）
- 同期プロトコル：HTTPS REST + MCP（Model Context Protocol）

---

## 貢献者

- Vin（miou1107）

Issues、PR、利用フィードバック歓迎。
リポジトリ：[github.com/miou1107/OwnMind](https://github.com/miou1107/OwnMind)（公開済なら）

---

## ライセンス

MIT
