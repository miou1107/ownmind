node_modules/
.env
.env.*
.DS_Store
.last-update-check
logs/

# v1.20：前端 client 編譯產物與依賴
client/node_modules/
client/dist/
client/.vite/
client/src/i18n/.translate-cache.json
# v1.20：build 產出後的 dashboard 靜態頁（並存於舊 src/public/index.html）
# 藍綠策略：新版進 dashboard/、舊 admin/ 跟 me/ 不動、退役才轉址
src/public/dashboard/
.claude/worktrees/
.claude/commands
.claude/skills
.claude/*.json
.claude/*.lock

# v1.19.10：repo 公開後敏感資料外洩 incident 後加強
# 任何含金鑰／密碼／證書／本機覆蓋設定的檔都不該進 repo
.mcp.local.json
credentials
credentials.*
*.pem
*.key
*.p12
*.pfx
secrets/
.env.local
.env.production
.env.staging

# v1.26.53：scripts/backup-db.sh 的 pg_dump 產物
# 這些檔含全部使用者的 api_key 明文、絕對不能進 repo
backups/
*.sql.gz

# v1.26.92 — per-session edit-reminder throttle state, written into the checkout at runtime
state/
