# 純為了減小 Docker context、不影響 runtime（這些路徑都不會被 server 端讀）
# 若未來 server code 新增讀檔路徑、要從這份排除清單拿掉
node_modules
client/node_modules
client/dist
.git
.github
.claude
.ownmind
# openspec / tests / docs / *.md：純文件、server runtime 不需要
openspec
tests
docs
*.md
# v1.26.126：例外。src/utils/changelog.js 在啟動時讀這一份，供 GET /api/changelog 用。
# 少了這行 re-include，Dockerfile 的 COPY CHANGELOG.md 會直接讓 build 失敗
#（failed to compute cache key: "/CHANGELOG.md": not found）——不是「頁尾空的」而已。
!CHANGELOG.md
.env
.env.*
.DS_Store
logs
